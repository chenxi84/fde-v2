# 已登记为「待业务/口径决策」的失败项 —— 这些**已知且已记账**，不判为新回归。
# 只有**不在本清单里**的 FAIL 才会让门禁变红（这样闸只为新问题报警，
# 不会因为几项长期待决策而常红 —— 常红的闸会训练人无视红灯）。
# 每增一条都必须同时在台账里记账：app/psc/BUGS_psc_<日期>.md
#
# ⚠ 修好一项就必须**立刻从本清单删掉** —— 留在清单里的已修项会**掩盖它日后的回归**。
#
# ── 当前：**空** ──
# 最后一项 `S5 触发方传参覆盖 BR-06 分档入参`（B-08）已于 2026-09-17 修复：
# `inventory_projection.scan_alert` 现在把水位与击穿余额传给 `demand_pool.create`，
# BR-06 的富余档（补到组批水位 B）在生产路径上可达，该判据转 PASS，已按纪律删除。
# 遗留的一半（BR-07「产能松紧」标记系统内无产生方 ⇒ 紧张档进不去）不是失败项、
# 判据也查不到，已在 `demand_pool/应用详设.md` 的三要素与台账里如实登记。

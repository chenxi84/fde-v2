# 已登记为「待业务/口径决策」的结构检查失败项（每行一个**靶点前缀**，# 注释）。
# 纪律同 psc_oracles_known.txt：修好一项必须立刻删掉 —— 已修项留在清单里会掩盖它日后的回归。
#
# ── B-08 已于 2026-09-17 修复（按 BR-06/BR-07 接线）──
# `inventory_projection.scan_alert` 现在把水位与击穿余额传给 `demand_pool.create`，
# 富余档（补到组批水位 B）在**生产路径上可达**；本清单里的
# `demand_pool.create 敏感参数全部调用点缺省` 已按纪律删除（它现在 PASS）。
# ⚠ **仍未实现的那一半**：BR-07 的「产能松紧」标记**系统内没有任何产生方**，
# 所以生产上恒为默认的**富余档**，**紧张档依旧进不去**（本判据查不到这种"另一端不可达"，
# 因为 `ctx.get("capacity_tight", False)` 被算作有兜底来源）。已在 `demand_pool/应用详设.md`
# 的 BR-06/BR-07 三要素里如实标注，并在台账记账 —— **不是"已全部实现"。**

# ── T1 逐参一致（《测试用例.md》→ 脚本）──
# 规范依据：design-plus/测试执行.md「入参以签名为准」**不等于可以删参数**。
# 这三处都是**存量**，按「新增/改动必须满足、存量登记待收敛」的口径登记。
# （T1 已于 2026-09-17 修好：TC-ERR-20/21 补全分档入参 → 按纪律从清单删除）

# ── T3 详设 BR 三要素完整性（第②步门禁）──
# 规范：design-plus/应用设计.md「三要素与载体格式无关」——每条 BR 的 `依赖输入`/`输出`/`生效点`
# 必须能从详设里**机器读出**（它们是第②步问「输入由谁产生」、第④步查字段级覆盖、
# 第③步查链上实参覆盖的**唯一挂钩点**）。
# 2026-09-17：**全组 17 个应用、全部 BR 已补齐**（先做 6 个出问题的，当晚铺完其余 11 个；
# ⚠ 条数别写死在这里 —— 它会随 BR 增删而漂（曾写 192，实为 193）。要数字看 `br_meta_check.py`
#   打印的「逐应用（核到/声明）」与 `br_coverage.py` 的「合计 N 条 BR」。
# `sales_history` 那份精简载体也重排成了编号小节载体）——**本判据现在全绿，清单为空**。
# 按纪律：修好即删（上面那 10 行已随补齐删除）；今后新增应用/新增 BR 缺三要素会当场变红。
#
# ── T4 BR 输出字段级覆盖（第④步门禁）──
# 判据：每条 BR 的 `输出` 字段必须被至少一条用例的**断言**引用过（「有编号对应用例」不算覆盖）。
# 取证：scripts/br_coverage.py（表在 app/psc/测试用例.md §4）。
# 全组 BR 已全部覆盖（2026-09-17 晚；条数以 `br_coverage.py` 打印为准）：原剩 2 条（`strategy_fitting BR-09/BR-10`，产物是简化实现编造的常量）
# 已随「库存侧参数改人工维护」的口径变更一并解决 —— 那两条的输出改成「本期不产出」，
# 新增 BR-20（库存侧参数取主数据人工值）并由链测试 TC-MC-18 断言「不被覆盖 + 镜像一致」。
# 原说明留档：
# （`_inventory_fit` 未落地：`inv_days` = `batch_window + safety/daily`、`changeover_cnt` = `len(history)//4`、
# `fulfill_rate` 恒 0.95）—— **给常量补断言 = 把 stub 固化成标准**，那比空着更坏，
# 故登记待「库存拟合真正落地」后删掉本行（届时补的是真断言）。
# 缺口已由 strategy_fitting 详设的三要素（⚠ 生效点缺失）与台账记账。

# ── T2 空转断言（第⑤步门禁）──
# 目标名**带组前缀**（`T2 [组] …`），因为登记一个组的存量不该把**别的组**也一起静音
# —— 那正是 known 基线最该防的事。
# `app/e2e` 是 2026-09-17 新写的产物，**全部 TC 步都有业务断言**（2026-09-18 复核为 35 步），不在此列。
# `app/psc` 是存量：曾 106 个 TC 步里有 11 处是"无任何断言"的（多为造数/前置步）—— 已随迭代收敛。
#   ⚠ 两个数都别写死在注释里（e2e 曾写 34、实为 35；psc 曾写 102、实为 106）：
#   要数字就现算 `grep -c 'step("TC-' app/<组>/tests/verify_chain_<组>*.py`。
# （T2 [psc] 已于 2026-09-17 修好：11 处空转断言全部补上业务断言 → 按纪律从清单删除）

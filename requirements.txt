# FDE v2 平台依赖
# 安装：pip install -r requirements.txt

# 核心（必须）
flask>=2.0
waitress>=2.0
cryptography>=3.0

# 可选：配置管理（.env 文件支持）
python-dotenv>=1.0

# 可选：PostgreSQL（Docker 部署用，不装则默认 SQLite）
psycopg2-binary>=2.9

# 可选：AI Agent 对话（兼容 OpenAI 接口的模型，如 DeepSeek / 通义千问 / 智谱）
openai>=1.0

# 可选：定时任务
apscheduler>=3.10

# 可选：构建流水线（/groupbuild 九步法）
langgraph>=0.2,<0.3
langgraph-checkpoint>=2.0,<3.0
langgraph-checkpoint-sqlite>=2.0

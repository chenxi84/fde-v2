# FDE v2 平台依赖
# 安装：pip install -r requirements.txt

# 核心（必须，缺一平台无法正常运行）
flask>=2.0
waitress>=2.0
cryptography>=3.0
python-dotenv>=1.0
agentscope[service,storage-sql]==2.0.6
aiosqlite>=0.19
tzlocal>=5.0
openai>=1.0
openpyxl>=3.0
xlrd>=2.0
apscheduler>=3.10
markdown>=3.0
sqlglot>=25.0

# 可选：PostgreSQL 驱动（仅 Docker/PostgreSQL 部署需要，默认 SQLite 无需）
psycopg2-binary>=2.9

# FDE v2 平台依赖
# 安装：pip install -r requirements.txt

# 核心（必须，缺一平台无法正常运行）
flask>=2.0
waitress>=2.0
cryptography>=3.0
python-dotenv>=1.0
agentscope[service,storage-sql]==2.0.6
aiosqlite>=0.19
tzlocal>=5.0
openai>=1.0
openpyxl>=3.0
xlrd>=2.0
apscheduler>=3.10
markdown>=3.0
sqlglot>=25.0
statsforecast==2.0.3

# 可选：PostgreSQL 驱动（仅 Docker/PostgreSQL 部署需要，默认 SQLite 无需）
psycopg2-binary>=2.9

# 可选：知识图谱（LightRAG 语义问答 + 本地 BGE embedding + 可视化）
# 未安装则 Agent 的「图谱问答」工具自动降级（平台其余功能不受影响）；不需要可删除这三行。
# 注意：sentence-transformers 依赖 torch，体积较大（约数 GB）。
lightrag-hku>=1.5
sentence-transformers>=3.0
pyvis>=0.3

# FDE v2 平台依赖
# 安装：pip install -r requirements.txt

# 核心（必须，缺一平台无法正常运行）
flask>=2.0
waitress>=2.0
cryptography>=3.0
python-dotenv>=1.0
agentscope[service,storage-sql]==2.0.6
aiosqlite>=0.19
tzlocal>=5.0
openai>=1.0
openpyxl>=3.0
xlrd>=2.0
apscheduler>=3.10
markdown>=3.0
sqlglot>=25.0
statsforecast==2.1.1

# 可选：PostgreSQL 驱动（仅 Docker/PostgreSQL 部署需要，默认 SQLite 无需）
psycopg2-binary>=2.9
# 部署侧取连接方式（PG/MySQL）：池的 pre-ping / recycle / 等待超时 / 溢出（2026-09-26 加）。
# 本地 SQLite 开发**不依赖**它 —— 未安装时自动回落 psycopg2 原生池（见 db._pg_pool 的 ImportError 分支）。
sqlalchemy>=2.0

# 可选：知识图谱（LightRAG 语义问答 + 可视化）
# 未安装则 Agent 的「图谱问答」工具自动降级（平台其余功能不受影响）；不需要可删除。
# embedding 走独立服务（见 requirements-embed.txt），主进程不装 torch。
lightrag-hku>=1.5
pyvis>=0.3

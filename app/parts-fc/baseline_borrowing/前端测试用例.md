# 应用组 `parts-fc` · baseline_borrowing 前端测试用例（基线借用）

> 产出技能：前端测试用例生成（第⑧步，规格 design/前端测试.md）。
> 页面：`parts-fc:baseline_borrowing`（本文件全部用例针对本页）。输入：本应用前端详设 + _contracts.md。
> 执行：第⑨步翻译成 app/parts-fc/tests/verify_view_parts-fc_baseline_borrowing.py。

## §0 测试数据字典（冻结 · 本脚本自足）

| 别名 | 类型 | 造数服务 | 关键参数 |
|---|---|---|---|
| BB1 | 待审核借用单·先导指标 | baseline_borrowing.create | part_no=BB-PART-001, veh_model=BB-MODEL-A, hist_months=3, borrow_method=先导指标, borrow_source=V2026-08 OEM滚动预测, source_params={"peak":450}, derived_qty={"2026-09":430}, proc_batch=BB-BATCH-001, calibration=首3月校准 |
| BB2 | 待审核借用单·池化 | baseline_borrowing.create | part_no=BB-PART-002, veh_model=BB-MODEL-B, hist_months=6, borrow_method=池化, borrow_source=平台层统计, source_params={"weight":0.3}, derived_qty={"2026-09":350}, proc_batch=BB-BATCH-001, calibration=半年度校准 |
| BB3 | 待审核借用单（供关闭） | baseline_borrowing.create | part_no=BB-PART-003, veh_model=BB-MODEL-C, hist_months=2, borrow_method=类比, borrow_source=MODEL-X, source_params={"ratio":0.8}, derived_qty={"2026-09":280}, proc_batch=BB-BATCH-001, calibration=早期校准 |

> BB1/BB2 覆盖两种借法；BB3 用于审核通过后关闭测试。proc_batch 为任意 D04 批次编号（仅标识关联，不校验存在性）。

### 跨应用前置造数（本字典自足）
| 别名 | 类型 | 造数服务 | 关键参数 |
|---|---|---|---|
| BB-VM1 | 车型映射 | vehicle_part_map.create | part_no=BB-PART-001, veh_model=BB-MODEL-A, usage=1, share=100, lc_stage=爬坡, lc_shape=传统, sop=2025-01, source=BOM |
| BB-VM2 | 车型映射 | vehicle_part_map.create | part_no=BB-PART-002, veh_model=BB-MODEL-B, usage=2, share=55, lc_stage=成熟, lc_shape=传统, sop=2024-06, source=BOM |
| BB-VM3 | 车型映射 | vehicle_part_map.create | part_no=BB-PART-003, veh_model=BB-MODEL-C, usage=1, share=100, lc_stage=成熟, lc_shape=传统, sop=2024-06, source=BOM |
| BB-DP | 加工批次 | demand_processing.create_batch | fcst_version=V2026-08, oem_code=OEM-A, plant_code=PLANT-1 |

> BB-DP 提供 proc_batch 引用；实际借用单创建时 proc_batch 参数传 BB-DP 产出的批次号。

## §1 本页渲染（防粘滞）

#### TC-VW-baseline_borrowing-01 路由渲染与防粘滞
- **页面**: parts-fc:baseline_borrowing
- **前置造数**: 无
- **操作**:
  1. 登录 admin → location.hash = '#/baseline_borrowing'
  2. 等待 `main .card` 出现
- **断言**:
  - `main .card` 数 > 0
  - **非看板页 `.kpi` 数 == 0**
  - 页面标题含「基线借用」

## §2 造数后列表有数据

#### TC-VW-baseline_borrowing-02 列表含所造借用单
- **页面**: parts-fc:baseline_borrowing
- **前置造数**:
  - 调 `baseline_borrowing.create(BB1)` 先导指标借用
  - 调 `baseline_borrowing.create(BB2)` 池化借用
  - 调 `baseline_borrowing.create(BB3)` 类比借用
  - BB1 调 `review(jy_no, approved=true, comment=通过)` → status=生效
  - BB2 调 `review(jy_no, approved=true, comment=通过)` → status=生效
  - BB2 调 `close(jy_no, reason=历史转充足)` → status=已转自产
- **操作**:
  1. 进入 `#/baseline_borrowing`
- **断言**:
  - 列表行数 >= 3
  - jy_no / part_no / borrow_method / status 列非空
  - BB1 status=生效, BB2 status=已转自产, BB3 status=待审核
  - hist_months < 12 的行橙色标记

#### TC-VW-baseline_borrowing-03 过滤条件生效
- **页面**: parts-fc:baseline_borrowing
- **前置造数**: 同 TC-VW-baseline_borrowing-02
- **操作**:
  1. status chips 选择「待审核」
- **断言**:
  - 结果仅 BB3（待审核）
  - borrow_method 下拉选「池化」→ 仅 BB2

## §3 模态全字段

#### TC-VW-baseline_borrowing-04 详情模态全字段+derived_qty 结构化
- **页面**: parts-fc:baseline_borrowing
- **前置造数**: 同 TC-VW-baseline_borrowing-02（用 BB1）
- **操作**:
  1. 点击 BB1 的 jy_no `.b-link`
  2. `wait_modal()`
- **断言**:
  - `.modal-bd.first` 含：jy_no / part_no / veh_model / hist_months / borrow_method / borrow_source
  - source_params 结构化键值对展示（tryParse）
  - calibration 可见
  - derived_qty 结构化期值表（tryParse：期间/基线值）
  - proc_batch / reviewer / review_date / created_at / updated_at
  - 「原始数据」可展开
  - BB2（已转自产）模态页脚无操作按钮

## §4 表单落库回显

#### TC-VW-baseline_borrowing-05 创建表单落库
- **页面**: parts-fc:baseline_borrowing
- **前置造数**: 无（独立）
- **操作**:
  1. 点击【+ 新建借用单】→ 填 part_no=BB-NEW-001, veh_model=BB-MODEL-A, hist_months=3, borrow_method=先导指标, borrow_source=V2026-10, source_params={"peak":500}, derived_qty={"2026-10":480}, proc_batch=BB-BATCH-001, calibration=早期校准
  2. 提交
- **断言**:
  - toast 含新 jy_no
  - 列表出现新记录，status=待审核
  - hist_months > 0 不填 calibration → toast 拦截

#### TC-VW-baseline_borrowing-06 审核与关闭
- **页面**: parts-fc:baseline_borrowing
- **前置造数**: 依赖 BB3（待审核）
- **操作（审核通过）**:
  1. BB3 → 【审核】→ approved=true, comment=审核通过 → 提交
- **断言**:
  - status=生效, reviewer/review_date 回填
- **操作（关闭）**:
  1. 审核后的 BB3 → 【关闭】→ reason=已切换自产 → 提交
- **断言**:
  - status=已关闭 或 已转自产

## §6 本会话 0 报错

- 全程 console error == 0 · pageerror == 0 · HTTP >= 400 == 0（favicon 除外）

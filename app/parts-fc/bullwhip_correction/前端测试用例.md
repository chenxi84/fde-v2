# 应用组 `parts-fc` · bullwhip_correction 前端测试用例（牛鞭修正）

> 产出技能：前端测试用例生成（第⑧步，规格 design/前端测试.md）。
> 页面：`parts-fc:bullwhip_correction`（本文件全部用例针对本页）。输入：本应用前端详设 + _contracts.md。
> 执行：第⑨步翻译成 app/parts-fc/tests/verify_view_parts-fc_bullwhip_correction.py。

| VPM-BW-001 | vehicle_part_map映射 | BW-PART-001 → VEH-A | 调用 `vehicle_part_map.create(part_no=BW-PART-001, veh_model=VEH-A, usage=1, share=100, lc_stage="成熟", lc_shape="传统", sop="2024-03-01", source="BOM")` |
| VPM-BW-002 | vehicle_part_map映射 | BW-PART-002 → VEH-A | 调用 `vehicle_part_map.create(part_no=BW-PART-002, veh_model=VEH-A, usage=1, share=100, lc_stage="成熟", lc_shape="传统", sop="2024-03-01", source="BOM")` |
| VPM-BW-003 | vehicle_part_map映射 | BW-PART-003 → VEH-A | 调用 `vehicle_part_map.create(part_no=BW-PART-003, veh_model=VEH-A, usage=1, share=100, lc_stage="成熟", lc_shape="传统", sop="2024-03-01", source="BOM")` |

## §0 测试数据字典（冻结 · 本脚本自足）

| 别名 | 类型 | 造数服务 | 关键参数 |
|---|---|---|---|
| BW1 | 待处理·容忍内 | bullwhip_correction.create | part_no=BW-PART-001, period=2026-09, tier=Tier1直供, derived_qty=990, end_qty=970, end_source=结算 |
| BW2 | 待处理·超阈 | bullwhip_correction.create | part_no=BW-PART-002, period=2026-09, tier=Tier2, derived_qty=1500, end_qty=1000, end_source=OEM消耗 |
| BW3 | 待处理·容忍内（供确认） | bullwhip_correction.create | part_no=BW-PART-003, period=2026-09, tier=Tier1直供, derived_qty=500, end_qty=490, end_source=结算 |

> amp_factor/amp_verdict 由系统自动计算；BW1 应判定为容忍内·维持，BW2 应判定为超阈·修正。

## §1 本页渲染（防粘滞）

#### TC-VW-bullwhip_correction-01 路由渲染与防粘滞
- **页面**: parts-fc:bullwhip_correction
- **前置造数**: 无
- **操作**:
  1. 登录 admin → location.hash = '#/bullwhip_correction'
  2. 等待 `main .card` 出现
- **断言**:
  - `main .card` 数 > 0
  - **非看板页 `.kpi` 数 == 0**
  - 页面标题含「牛鞭修正」

## §2 造数后列表有数据

#### TC-VW-bullwhip_correction-02 列表含所造处理单
- **页面**: parts-fc:bullwhip_correction
- **前置造数**:
  - 调 `bullwhip_correction.create(BW1)` 容忍内
  - 调 `bullwhip_correction.maintain(bw_no=BW1.bw_no)` → status=维持
  - 调 `bullwhip_correction.create(BW2)` 超阈
  - 调 `bullwhip_correction.create(BW3)` 容忍内
- **操作**:
  1. 进入 `#/bullwhip_correction`
- **断言**:
  - 列表行数 >= 3
  - bw_no / part_no / period / tier / derived_qty / end_qty / amp_factor / amp_verdict / status 列非空
  - BW1 amp_verdict=容忍内·维持, status=维持
  - BW2 amp_verdict=超阈·修正, status=待处理

#### TC-VW-bullwhip_correction-03 过滤条件生效
- **页面**: parts-fc:bullwhip_correction
- **前置造数**: 同 TC-VW-bullwhip_correction-02
- **操作**:
  1. status chips 选择「待处理」
  2. tier 下拉选择「Tier2」
- **断言**:
  - 结果仅 BW2

## §3 模态全字段

#### TC-VW-bullwhip_correction-04 详情模态全字段
- **页面**: parts-fc:bullwhip_correction
- **前置造数**: 同 TC-VW-bullwhip_correction-02（用 BW2）
- **操作**:
  1. 点击 BW2 bw_no `.b-link` → `wait_modal()`
- **断言**:
  - `.modal-bd.first` 含：bw_no / part_no / period / tier / derived_qty / end_qty / end_source / amp_factor / amp_verdict / corr_action / final_qty / checker / chk_date / status / created_at / updated_at
  - amp_factor 与 amp_verdict 高亮（超阈红色）
  - 「原始数据」可展开
  - BW1（维持）模态页脚含【确认】按钮
  - 已确认处理单模态页脚无动作按钮

## §4 表单落库回显

#### TC-VW-bullwhip_correction-05 创建处理单落库
- **页面**: parts-fc:bullwhip_correction
- **前置造数**: 无
- **操作**:
  1. 点击【+ 新建处理单】→ 填 part_no=BW-NEW-001, period=2026-10, tier=Tier1直供
  2. 展开「更多字段」填 derived_qty=800, end_qty=780, end_source=结算
  3. 提交
- **断言**:
  - toast 含新 bw_no
  - 列表出现新记录，status=待处理，amp_factor/amp_verdict 已计算

#### TC-VW-bullwhip_correction-06 维持操作
- **页面**: parts-fc:bullwhip_correction
- **前置造数**: 依赖 BW3（容忍内）
- **操作**:
  1. BW3 行【维持】
- **断言**:
  - status=维持

#### TC-VW-bullwhip_correction-07 修正操作与六机制校验
- **页面**: parts-fc:bullwhip_correction
- **前置造数**: 依赖 BW2（超阈）
- **操作**:
  1. BW2 行【修正】→ 弹出子表单
  2. 勾选六机制（终端需求可见 + 信息共享上传）+ 填说明文字
  3. 填 final_qty=1000
  4. 提交
- **断言**:
  - status=已修正，corr_action 含机制+说明，final_qty=1000
  - 仅填 final_qty 不选机制 → toast 警告「禁止无机制纯减数」

#### TC-VW-bullwhip_correction-08 确认操作
- **页面**: parts-fc:bullwhip_correction
- **前置造数**: 依赖 BW1（维持状态）
- **操作**:
  1. BW1 行【确认】
- **断言**:
  - status=已确认（转发布）

## §6 本会话 0 报错

- 全程 console error == 0 · pageerror == 0 · HTTP >= 400 == 0（favicon 除外）

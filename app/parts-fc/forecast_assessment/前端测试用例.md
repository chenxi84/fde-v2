# 应用组 `parts-fc` · forecast_assessment 前端测试用例（预测考核）

> 产出技能：前端测试用例生成（第⑧步，规格 design/前端测试.md）。
> 页面：`parts-fc:forecast_assessment`（本文件全部用例针对本页）。输入：本应用前端详设 + _contracts.md。
> 执行：第⑨步翻译成 app/parts-fc/tests/verify_view_parts-fc_forecast_assessment.py。

| VPM-FA-001 | vehicle_part_map映射 | FA-PART-001 → VEH-A | 调用 `vehicle_part_map.create(part_no=FA-PART-001, veh_model=VEH-A, usage=1, share=100, lc_stage="成熟", lc_shape="传统", sop="2024-03-01", source="BOM")` |
| VPM-FA-002 | vehicle_part_map映射 | FA-PART-002 → VEH-A | 调用 `vehicle_part_map.create(part_no=FA-PART-002, veh_model=VEH-A, usage=1, share=100, lc_stage="成熟", lc_shape="传统", sop="2024-03-01", source="BOM")` |

## §0 测试数据字典（冻结 · 本脚本自足）

| 别名 | 类型 | 造数服务 | 关键参数 |
|---|---|---|---|
| FA1 | 考核期批次 | forecast_assessment.create | period=2026-08 |
| FA2 | 考核期批次（供归因） | forecast_assessment.create | period=2026-09 |
| REC1 | 考核记录（FA1） | forecast_assessment.add_record | fa_no=FA1.fa_no, part_no=FA-PART-001, veh_model=FA-MODEL-A, period=2026-08, fcst_qty=1200, actual_qty=1190, settle_qty=1195, base_qty=1180, adj_qty=1200 |
| REC2 | 考核记录（FA2，供归因） | forecast_assessment.add_record | fa_no=FA2.fa_no, part_no=FA-PART-002, veh_model=FA-MODEL-B, period=2026-09, fcst_qty=1000, actual_qty=800, settle_qty=810, base_qty=950, adj_qty=1050 |

> REC1 conv_rate≈0.992 正常；REC2 conv_rate=0.8 偏差大（归因测试用）。

## §1 本页渲染（防粘滞）

#### TC-VW-forecast_assessment-01 路由渲染与防粘滞
- **页面**: parts-fc:forecast_assessment
- **前置造数**: 无
- **操作**:
  1. 登录 admin → location.hash = '#/forecast_assessment'
  2. 等待 `main .card` 出现
- **断言**:
  - `main .card` 数 > 0
  - **非看板页 `.kpi` 数 == 0**
  - 页面标题含「预测考核」

## §2 造数后列表有数据

#### TC-VW-forecast_assessment-02 列表含所造考核记录
- **页面**: parts-fc:forecast_assessment
- **前置造数**:
  - 调 `forecast_assessment.create(FA1)`
  - 调 `forecast_assessment.add_record(REC1)`
  - 调 `forecast_assessment.create(FA2)`
  - 调 `forecast_assessment.add_record(REC2)`
  - 调 `forecast_assessment.attribute(fa_no=REC2.fa_no, part_no=FA-PART-002, period=2026-09, attribution=OEM需求塌方:客户实际订单量萎缩30%, result=免责剔除, evidence=客户确认函)`
- **操作**:
  1. 进入 `#/forecast_assessment`
- **断言**:
  - 列表行数 >= 2
  - fa_no / part_no / period / fcst_qty / actual_qty / conv_rate / dev_qty / result 列非空
  - REC1 conv_rate≈0.992, REC2 result=免责剔除

#### TC-VW-forecast_assessment-03 过滤条件生效
- **页面**: parts-fc:forecast_assessment
- **前置造数**: 同 TC-VW-forecast_assessment-02
- **操作**:
  1. result 下拉选择「免责剔除」
- **断言**:
  - 结果仅 REC2

## §3 模态全字段

#### TC-VW-forecast_assessment-04 详情模态全字段+FVA面板
- **页面**: parts-fc:forecast_assessment
- **前置造数**: 同 TC-VW-forecast_assessment-02（用 REC1）
- **操作**:
  1. 点击 REC1 fa_no `.b-link` → `wait_modal()`
- **断言**:
  - `.modal-bd.first` 含：fa_no / part_no / veh_model / period / cycle / fcst_qty / actual_qty / conv_rate / dev_qty / settle_qty / base_qty / adj_qty / attribution / result / assessor / created_at / updated_at
  - FVA 面板异步加载：基线误差/核定误差/FVA（quiet 加载）
  - 「原始数据」可展开
  - REC2（已归因）模态页脚无动作按钮

## §4 表单落库回显

#### TC-VW-forecast_assessment-05 创建考核期+添加记录落库
- **页面**: parts-fc:forecast_assessment
- **前置造数**: 无
- **操作（创建）**:
  1. 点击【+ 新建考核】→ 填 period=2026-10 → 提交
- **断言**:
  - toast 成功，列表出现新批次
- **操作（添加记录）**:
  1. 新批次行 → 【添加记录】→ 填 part_no=FA-NEW-001, veh_model=FA-MODEL-A, period=2026-10, fcst_qty=1500, actual_qty=1450, settle_qty=1460, base_qty=1480, adj_qty=1500
  2. 提交
- **断言**:
  - toast「考核记录已添加」
  - 列表出现新记录，conv_rate/dev_qty 已计算

#### TC-VW-forecast_assessment-06 归因认定回显
- **页面**: parts-fc:forecast_assessment
- **前置造数**: 依赖 REC2（result 为空）
- **操作**:
  1. 新增一条待归因记录（result 为空）
  2. 该行【归因认定】→ 选择 attribution=OEM需求塌方 + 填说明 + result=免责剔除 + evidence=客户确认函
  3. 提交
- **断言**:
  - toast「归因认定完成」
  - result=免责剔除
  - 行变为终态不可再编辑

#### TC-VW-forecast_assessment-07 批量计算
- **页面**: parts-fc:forecast_assessment
- **前置造数**: 依赖 FA1（已有记录）
- **操作**:
  1. 列表工具栏【批量计算】→ 填 period=2026-08 → 提交
- **断言**:
  - toast「批量计算完成 · N 条记录已更新」

## §6 本会话 0 报错

- 全程 console error == 0 · pageerror == 0 · HTTP >= 400 == 0（favicon 除外；FVA quiet 加载 4xx 不视为错误）

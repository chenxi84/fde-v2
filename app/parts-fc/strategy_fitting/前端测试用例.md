# 应用组 `parts-fc` · strategy_fitting 前端测试用例（策略仿真拟合）

> 产出技能：前端测试用例生成（第⑧步，规格 design/前端测试.md）。
> 页面：`parts-fc:strategy_fitting`（本文件全部用例针对本页）。输入：本应用前端详设 + _contracts.md。
> 执行：第⑨步翻译成 app/parts-fc/tests/verify_view_parts-fc_strategy_fitting.py。

## §0 测试数据字典（冻结 · 本脚本自足）

| 别名 | 类型 | 造数服务 | 关键参数 |
|---|---|---|---|
| SF1 | 拟合中·稳定形态 | strategy_fitting.create | part_no=SF-PART-001, veh_model=SF-MODEL-A, demand_shape=稳定, data_range=2025-01~2026-06 |
| SF2 | 拟合中·波动形态 | strategy_fitting.create | part_no=SF-PART-002, veh_model=SF-MODEL-B, demand_shape=波动, data_range=2025-06~2026-06 |
| SF3 | 已选定·季节形态 | strategy_fitting.create | part_no=SF-PART-003, veh_model=SF-MODEL-C, demand_shape=季节 |

> SF1 用于添加候选+回测+选定完整流程；SF2 用于列表多样性；SF3 用于终态展示。

### 跨应用前置造数（本字典自足）
| 别名 | 类型 | 造数服务 | 关键参数 |
|---|---|---|---|
| SF-VM1 | 车型映射 | vehicle_part_map.create | part_no=SF-PART-001, veh_model=SF-MODEL-A, usage=1, share=100, lc_stage=成熟, lc_shape=传统, sop=2025-01, source=BOM |
| SF-VM2 | 车型映射 | vehicle_part_map.create | part_no=SF-PART-002, veh_model=SF-MODEL-B, usage=2, share=55, lc_stage=成熟, lc_shape=传统, sop=2024-06, source=BOM |
| SF-VM3 | 车型映射 | vehicle_part_map.create | part_no=SF-PART-003, veh_model=SF-MODEL-C, usage=1, share=100, lc_stage=爬坡, lc_shape=传统, sop=2025-01, source=BOM |

## §1 本页渲染（防粘滞）

#### TC-VW-strategy_fitting-01 路由渲染与防粘滞
- **页面**: parts-fc:strategy_fitting
- **前置造数**: 无
- **操作**:
  1. 登录 admin → location.hash = '#/strategy_fitting'
  2. 等待 `main .card` 出现
- **断言**:
  - `main .card` 数 > 0
  - **非看板页 `.kpi` 数 == 0**
  - 页面标题含「策略拟合」

## §2 造数后列表有数据

#### TC-VW-strategy_fitting-02 列表含所造拟合单
- **页面**: parts-fc:strategy_fitting
- **前置造数**:
  - 调 `strategy_fitting.create(SF1)` 稳定拟合
  - 调 `strategy_fitting.create(SF2)` 波动拟合
  - 调 `strategy_fitting.create(SF3)` 季节拟合
  - SF3: add_candidate(fit_no, strategy=指数平滑 α=0.3, inv_policy=安全库存3天) → run_backtest → select_strategy(fit_no, cand_no) → status=生效中
- **操作**:
  1. 进入 `#/strategy_fitting`
- **断言**:
  - 列表行数 >= 3
  - fit_no / part_no / veh_model / demand_shape / status 列非空

#### TC-VW-strategy_fitting-03 过滤条件生效
- **页面**: parts-fc:strategy_fitting
- **前置造数**: 同 TC-VW-strategy_fitting-02
- **操作**:
  1. status 下拉选择「拟合中」
- **断言**:
  - 结果仅 SF1/SF2（拟合中），不含 SF3（生效中）

## §3 模态全字段

#### TC-VW-strategy_fitting-04 详情模态全字段+候选子表
- **页面**: parts-fc:strategy_fitting
- **前置造数**: 依赖 SF1（拟合中，先 add_candidate 2条 + run_backtest）
- **操作**:
  1. 调 `strategy_fitting.add_candidate(fit_no=SF1.fit_no, strategy=指数平滑 α=0.3, inv_policy=安全库存3天/周补货)`
  2. 调 `strategy_fitting.add_candidate(fit_no=SF1.fit_no, strategy=移动平均 w=4, inv_policy=安全库存5天/周补货)`
  3. 调 `strategy_fitting.run_backtest(fit_no=SF1.fit_no)`
  4. 点击 SF1 fit_no `.b-link` → `wait_modal()`
- **断言**:
  - `.modal-bd.first` 含：fit_no / part_no / veh_model / demand_shape / data_range / backtest_spec / status / fitter / fit_date / created_at / updated_at
  - 候选明细子表：cand_no / strategy / inv_policy / mape / bias / service_level / inv_cost / score / rank / selected
  - rank=1 绿色高亮
  - 「原始数据」可展开

## §4 表单落库回显

#### TC-VW-strategy_fitting-05 创建拟合单落库
- **页面**: parts-fc:strategy_fitting
- **前置造数**: 无
- **操作**:
  1. 点击【+ 新建拟合】→ 填 part_no=SF-PART-001, veh_model=SF-MODEL-A, demand_shape=稳定, data_range=2025-01~2026-08
  2. 提交
- **断言**:
  - toast 含新 fit_no
  - 列表出现新记录，status=拟合中

#### TC-VW-strategy_fitting-06 添加候选+回测+选定
- **页面**: parts-fc:strategy_fitting
- **前置造数**: 依赖 SF1（拟合中）
- **操作（添加候选）**:
  1. SF1 模态 → 【添加候选】→ 填 strategy=指数平滑 α=0.3, inv_policy=安全库存3天 → 提交
  2. 再添加第二条：strategy=移动平均 w=4, inv_policy=安全库存5天 → 提交
- **断言**: 候选子表新增 2 行
- **操作（回测）**:
  1. 【执行回测】→ 等待加载完成
- **断言**: 候选子表 mape/bias/service_level/inv_cost/score/rank 列填充
- **操作（选定）**:
  1. rank=1 行【选定策略】
- **断言**: toast 成功，status=生效中，selected ★标记

## §6 本会话 0 报错

- 全程 console error == 0 · pageerror == 0 · HTTP >= 400 == 0（favicon 除外）

# 应用组 `parts-fc` · common_parts_agg 前端测试用例（通用件汇总修正）

> 产出技能：前端测试用例生成（第⑧步，规格 design/前端测试.md）。
> 页面：`parts-fc:common_parts_agg`（本文件全部用例针对本页）。输入：本应用前端详设 + _contracts.md。
> 执行：第⑨步翻译成 app/parts-fc/tests/verify_view_parts-fc_common_parts_agg.py。

| VPM-CA-001 | vehicle_part_map映射 | CA-PART-001 → VEH-A | 调用 `vehicle_part_map.create(part_no=CA-PART-001, veh_model=VEH-A, usage=1, share=100, lc_stage="成熟", lc_shape="传统", sop="2024-03-01", source="BOM")` |
| VPM-CA-002 | vehicle_part_map映射 | CA-PART-002 → VEH-A | 调用 `vehicle_part_map.create(part_no=CA-PART-002, veh_model=VEH-A, usage=1, share=100, lc_stage="成熟", lc_shape="传统", sop="2024-03-01", source="BOM")` |
| VPM-CA-003 | vehicle_part_map映射 | CA-PART-003 → VEH-A | 调用 `vehicle_part_map.create(part_no=CA-PART-003, veh_model=VEH-A, usage=1, share=100, lc_stage="成熟", lc_shape="传统", sop="2024-03-01", source="BOM")` |

## §0 测试数据字典（冻结 · 本脚本自足）

| 别名 | 类型 | 造数服务 | 关键参数 |
|---|---|---|---|
| CA1 | 待修正汇总单 | common_parts_agg.create | part_no=CA-PART-001, period=2026-09 |
| CA2 | 待修正汇总单（供确认） | common_parts_agg.create | part_no=CA-PART-002, period=2026-09 |
| CA3 | 已修正汇总单 | common_parts_agg.create | part_no=CA-PART-003, period=2026-09 |

> CA1 用于去重修正+添加明细；CA2 用于确认流转；CA3 先 set_dedup 再 confirm 成为已确认终态。

## §1 本页渲染（防粘滞）

#### TC-VW-common_parts_agg-01 路由渲染与防粘滞
- **页面**: parts-fc:common_parts_agg
- **前置造数**: 无
- **操作**:
  1. 登录 admin → location.hash = '#/common_parts_agg'
  2. 等待 `main .card` 出现
- **断言**:
  - `main .card` 数 > 0
  - **非看板页 `.kpi` 数 == 0**
  - 页面标题含「通用件汇总」

## §2 造数后列表有数据

#### TC-VW-common_parts_agg-02 列表含所造汇总单
- **页面**: parts-fc:common_parts_agg
- **前置造数**:
  - 调 `common_parts_agg.create(CA1)` 待修正
  - 调 `common_parts_agg.create(CA2)` 待修正
  - 调 `common_parts_agg.create(CA3)` 
  - 调 `common_parts_agg.set_dedup(agg_no=CA3.agg_no, dedup_qty=1000, dedup_reason=差异化折减：口头加码合计800经历史实际回测折减至750，理由充分依据完整)` → status=已修正
  - 调 `common_parts_agg.confirm(agg_no=CA3.agg_no)` → status=已确认
- **操作**:
  1. 进入 `#/common_parts_agg`
- **断言**:
  - 列表行数 >= 3
  - agg_no / part_no / period / sum_qty / status 列非空
  - CA1/CA2 status=待修正, CA3 status=已确认

#### TC-VW-common_parts_agg-03 过滤条件生效
- **页面**: parts-fc:common_parts_agg
- **前置造数**: 同 TC-VW-common_parts_agg-02
- **操作**:
  1. status 下拉选择「待修正」
- **断言**:
  - 结果仅 CA1/CA2

## §3 模态全字段

#### TC-VW-common_parts_agg-04 详情模态全字段+明细子表
- **页面**: parts-fc:common_parts_agg
- **前置造数**: 依赖 CA1（先 add_detail + set_dedup）
- **操作**:
  1. 调 `common_parts_agg.add_detail(agg_no=CA1.agg_no, oem_code=OEM-A, owner_sales=S001, approved_qty=500, evidence_qty=100, verbal_qty=200)`
  2. 调 `common_parts_agg.set_dedup(agg_no=CA1.agg_no, dedup_qty=700, dedup_reason=口头加码200按历史折减至100，差异化折减依据充分且完整可供审计追溯)`
  3. 点击 CA1 agg_no `.b-link` → `wait_modal()`
- **断言**:
  - `.modal-bd.first` 含：agg_no / part_no / period / sum_qty / anchor_ref（tryParse 三锚）/ dedup_qty / dedup_pct / dedup_reason / split_qty / status / reviser / revise_time / created_at / updated_at
  - 客户份额明细子表：oem_code / owner_sales / approved_qty / evidence_qty / verbal_qty
  - 「原始数据」可展开
  - CA3（已确认）模态页脚无动作按钮

## §4 表单落库回显

#### TC-VW-common_parts_agg-05 创建汇总单落库
- **页面**: parts-fc:common_parts_agg
- **前置造数**: 无
- **操作**:
  1. 点击【+ 新建汇总】→ 填 part_no=CA-NEW-001, period=2026-10
  2. 提交
- **断言**:
  - toast 含新 agg_no
  - 列表出现新记录

#### TC-VW-common_parts_agg-06 去重修正与确认流转
- **页面**: parts-fc:common_parts_agg
- **前置造数**: 依赖 CA2（待修正）
- **操作（去重修正）**:
  1. CA2 模态 → 【去重修正】→ 填 dedup_qty=900, dedup_reason（>=50字符）
  2. 提交
- **断言**:
  - dedup_qty/dedup_pct/dedup_reason/reviser/revise_time 原地刷新
  - status→已修正
  - dedup_reason < 50字符 → toast 警告
- **操作（确认）**:
  1. 已修正后【确认】
- **断言**:
  - status→已确认

## §6 本会话 0 报错

- 全程 console error == 0 · pageerror == 0 · HTTP >= 400 == 0（favicon 除外）

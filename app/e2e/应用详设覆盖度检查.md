# 成员任务管理应用组（e2e）· 应用详设覆盖度检查报告（第②步）

> 产出时间：2026-09-17
> 依据：`app/e2e/architecture.md`（148 行：§1 聚合根清单总表 + §2 聚合根卡 ×2 + §3 聚合关系图 + §4 构建批次）
> 检查对象：`app/e2e/member/应用详设.md`（509 行）、`app/e2e/task/应用详设.md`（516 行）
> 规格：`design-plus/应用设计.md` + `design-plus/工具链使用说明.md` 第 4 步「▶ 完成门禁（硬性，未通过不得进入第 5 步）」

---

## 一、文件完整性

| # | 上游（`architecture.md` 聚合根卡） | 应产出 | 实际产出 | 行数 | 状态 |
|---|---|---|---|---|---|
| 1 | §2.1 `member` / `Member` | `<应用目录>/应用详设.md` | `app/e2e/member/应用详设.md` | 509 | ✅ |
| 2 | §2.2 `task` / `Task` | `<应用目录>/应用详设.md` | `app/e2e/task/应用详设.md` | 516 | ✅ |

- **详设产出覆盖度 = 2/2 = 100%** ✅（架构卡 2 张 → 详设 2 份，无遗漏、无多余）

同目录的第③步产物（本报告**仅登记、不判定**）：`member.py` 165 / `task.py` 267 / `schema.sql` 12、16 / `README.md` 165、140 / `HOWTOUSE.md` 25、27。

---

## 二、完成门禁逐项核对

> 门禁条款照抄《工具链使用说明.md》第 4 步「▶ 完成门禁」的 7 条，逐条对照 e2e 实际产物。
> ⚠ 门禁条文里写的节号（「§4.1 功能清单」「§7.3 外部依赖」「§3.2/§3.4」）是**编号小节载体**的节号；
> e2e 用的是**PRD 表格载体**，节号不同（详见 2.3 / 2.4 的脚注）。`design-plus/应用设计.md`
> 已把「**按标题找，不要按节号找**」写成纪律，本报告照此办理。

### 2.1 每个聚合根卡的**对外操作** → 对应详设中的 **US/FUNC 编号**

| 聚合根卡:对外操作 | 详设 US | 详设 FUNC | 判定 |
|---|---|---|---|
| `member.create(member_no, name, email, role)` | `US-1` 创建成员 | `FUNC-1` 创建成员 | ✅ |
| `member.get(member_no)` | `US-2` 查询成员详情 | `FUNC-2` 查询成员详情 | ✅ |
| `member.list(keyword?, role?)` | `US-3` 查询成员列表 | `FUNC-3` 查询成员列表 | ✅ |
| `task.create(title, description, assignee_member_no, priority)` | `US-1` 创建任务 | `FUNC-1` 创建任务 | ✅ |
| `task.get(task_no)` | `US-2` 查询任务 | `FUNC-2` 查询任务详情 | ✅ |
| `task.list(status?, assignee_member_no?, priority?)` | `US-3` 查询任务列表 | `FUNC-3` 查询任务列表 | ✅ |
| `task.start(task_no)` | `US-4` 启动任务 | `FUNC-4` 启动任务 | ✅ |
| `task.complete(task_no)` | `US-5` 完成任务 | `FUNC-5` 完成任务 | ✅ |
| `task.reopen(task_no)` | `US-6` 退回任务 | `FUNC-6` 退回任务 | ✅ |

- 逐条数过：架构卡共 **9 个对外操作**（member 3 + task 6），**9/9** 都能在详设里找到同名的 US 与 FUNC。
- 每个 US 下都写了 AC（Given/When/Then）：member `US-1-AC1..5` / `US-2-AC1..3` / `US-3-AC1..6`；task `US-1-AC1..4` / `US-2-AC1..2` / `US-3-AC1..5` / `US-4-AC1..4` / `US-5-AC1..3` / `US-6-AC1..3`——**共 35 条 AC**。
- **对外操作覆盖度 = 9/9 = 100%** ✅

### 2.2 每个聚合根卡的**关键不变量** → 对应详设中的 **BR 编号**

**`member` 卡（3 条不变量）**

| 架构卡:关键不变量 | 详设 BR（文件:节/编号） | 判定 |
|---|---|---|
| `member_no` 必填且全局唯一 | `member/应用详设.md` §3.4 `BR-1`（必填）+ `BR-2`（唯一） | ✅ |
| `role` 取值必须为 `admin` 或 `member` | 同上 §3.4 `BR-6` | ✅ |
| 创建后是否允许修改/删除：业务未提供，默认不开放 | 同上 §3.4 `BR-11` | ✅ |

**`task` 卡（7 条不变量）**

| 架构卡:关键不变量 | 详设 BR（文件:节/编号） | 判定 |
|---|---|---|
| `task_no` 必填且全局唯一 | `task/应用详设.md` §3.3 `BR-7` | ✅ |
| `title` 必填，为空时抛 `FdeError` | 同上 §3.3 `BR-1`（必填）+ `BR-2`（长度 ≤200） | ✅ |
| `priority` 取值必须为 `low` 或 `high` | 同上 §3.3 `BR-4` | ✅ |
| `status` 取值必须为 `待办`/`进行中`/`已完成` | 同上 §3.3 `BR-9` | ✅ |
| 创建时 `assignee_member_no` 对应成员必须存在（调 `member.get` 校验） | 同上 §3.3 `BR-6` | ✅ |
| 状态流转必须合法（`待办`只能 `start`／`进行中`可 `complete`/`reopen`／`已完成`不可再流转） | 同上 §3.3 `BR-10`、`BR-11`、`BR-12`、`BR-13` | ✅ |
| 非法状态流转抛 `FdeError` | 同上 §3.3 `BR-13`、`BR-14` | ✅ |

- **关键不变量覆盖度 = 10/10 = 100%** ✅
- **第②步增量（本步该补的逐字段校验）确实补了**：架构卡上只有边界不变量，详设在**业务规则表**里补出了字段级规则 —— member `BR-3/4/5/7/8/9/10`（姓名必填、邮箱必填/格式、get 的编号必填与不存在、list 的 role 枚举与空结果）、task `BR-2/3/5/15/16`（标题长度、描述长度、指派编号必填、更新时间、列表筛选枚举）。**BR 数（12 / 16）均 ≥ 卡上不变量数（3 / 7）**。

### 2.3 每个聚合根卡的**状态机** → 对应详设**「状态机」节**

| 聚合根卡:状态机 | 详设对应节（文件:节） | 逐条对照 | 判定 |
|---|---|---|---|
| `member`：卡写「无」 | `member/应用详设.md` **§3.5 状态机**（原文：「`member` 为主数据应用，无业务状态机。」+ `无状态流转`） | 无转换可对，声明一致 | ✅ |
| `task`：4 条转换 | `task/应用详设.md` **§3.2 状态机** | 见下 | ✅ |

`task` 逐条对照（架构卡 4 行 ↔ 详设 §3.2 代码块 4 行，**逐字一致**）：

| 架构卡 | 详设 §3.2 | 判定 |
|---|---|---|
| `待办 --start--> 进行中` | `待办 --start--> 进行中` | ✅ |
| `进行中 --complete--> 已完成` | `进行中 --complete--> 已完成` | ✅ |
| `进行中 --reopen--> 待办` | `进行中 --reopen--> 待办` | ✅ |
| `已完成 --禁止操作--> 已完成` | `已完成 --禁止 start/complete/reopen--> 已完成` | ✅ |

> **节号脚注**：e2e 的状态机在 **member §3.5 / task §3.2**，不是门禁条文里写的 §3.4 —— 因为 e2e 用 PRD 表格载体（member 的 §3.4 是业务规则、task 没有 §3.4）。`design-plus/应用设计.md` 已把这两种载体与三种 §3.x 布局写成表格备案（其中明确列了 `app/e2e/member` 与 `app/e2e/task` 两种布局）。

- **状态机覆盖度 = 2/2 应用 = 100%** ✅

### 2.4 每个聚合根卡的**跨应用调用** → 对应详设**外部依赖节（§7.3）**

| 聚合根卡:跨应用调用 | 详设对应（文件:节） | 判定 |
|---|---|---|
| `member`：卡写「无」 | `member/应用详设.md` **§4.5.1 本应用主动调用**（原文：「`member` 应用无主动跨应用调用。」+「无 `self.fde.call` 依赖」）；§4.5.2 另列**被** `task.create` 调用 | ✅ |
| `task`：`task.create` → `member.get`（校验指派成员是否存在） | `task/应用详设.md` **§4.6 跨应用依赖** 表 1 行：`task.create` → `member.get`，调用方式 `self.fde.call('member.get', member_no=assignee_member_no)`，含「失败处理」列 | ✅ |

- **跨应用调用覆盖度 = 2/2 应用 = 100%** ✅（本组只有 1 条跨应用调用）
- 详设还额外把「被谁调用」也画全了（`member` §4.5.2 列了调用方 `task`），与架构卡 §2.2 / §3 聚合关系图的 `task.assignee_member_no --> member.member_no` 一致。
- **节号脚注**：门禁条文写的是「§7.3 外部依赖」（编号小节载体的节号）；e2e 详设共 6 节（§1 需求概览 / §2 用户故事 / §3 业务场景 / §4 功能清单+字段表 / §5 权限矩阵 / §6 验收标准），**没有 §7 章**，外部依赖落在 §4.5 / §4.6。按标题找即命中。

### 2.5 BRD 中每张单据的**字段** → 对应详设 **§4 字段表**

> ⚠ 前置事实：`app/e2e/` **没有 `brd/` 目录**（详见「执行期发现」2）。故本项以第①步实际产物 `architecture.md` 的「关键属性」表充当上游（同样是「单据/实体字段」这一层）。

| 上游:关键属性（架构卡） | 详设 §4 字段表（文件:节） | 判定 |
|---|---|---|
| `member`：`member_no` / `name` / `email` / `role`（4 字段） | `member/应用详设.md` §4.2 表 `member` **5 列** = 4 字段 + `created_at` | ✅ 4/4 |
| `task`：`task_no` / `title` / `description` / `assignee_member_no` / `priority` / `status`（6 字段） | `task/应用详设.md` §4.2 表 `task` **8 列** = 6 字段 + `created_at` / `updated_at` | ✅ 6/6 |

- 详设字段表比架构卡**多出的列都带解释**：member §4.3 逐条说明（`created_at` 用于审计与排序）；task §4.2 的类型/约束列写明 `updated_at`「状态变更时由应用刷新」。
- member 卡上的 `role` 枚举（`admin`/`member`）在详设 §4.2 落成 `CHECK (role IN ('admin','member'))`；task 卡的 `priority`/`status` 枚举同样落成 `CHECK`。
- **字段覆盖度 = 10/10 = 100%** ✅

### 2.6 每条 BR 的 `依赖输入` **逐项有承载**（人工审查项）

> 门禁原文：问一句「这个输入**系统里由谁产生**」，答不上来的就是缺口 —— 实体字段 / 主数据 / 外部适配器 / 上游服务实参，四者之一才算。
> ⚠ 门禁自己写明：**这一步是人工审查项，不是脚本**（代码侧对应检查在第③步 `scan_structure.py`）。

| 范围 | 承载情形 | 判定 |
|---|---|---|
| member `BR-1`~`BR-10`（10 条） | 输入为**调用方入参**（`member_no`/`name`/`email`/`role`/`keyword`）或**本表现有行**（`BR-2` 唯一性、`BR-8` 存在性），逐条写明 | ✅ |
| task `BR-1`~`BR-7`、`BR-9`~`BR-14`、`BR-16`（14 条） | 输入为**入参**或**本表当前值**；其中 `BR-6` 写明「`assignee_member_no`（入参）+ **member 行（`member` 应用产生）**」—— 这是本组唯一「输入来自另一个应用」的规则，即上游服务实参 | ✅ |
| member `BR-11` / `BR-12`（2 条） | 详设 §3.4.1 表填的是「**—（填不出）**」，并在表下注释说明理由 | ⚠ |
| task `BR-8` / `BR-15`（2 条） | `BR-8` 写「—（固定值）」、`BR-15` 写「—（平台自动注入）」——**写法不在四类枚举内**，需人工核 | ⚠ |

**人工审查结论**：

- `member BR-11`（「成员创建后默认不开放修改/删除/停用」）与 `BR-12`（「`member` 不主动调用其他应用」）**确实答不上「输入由谁产生」**——因为**它们不是规则**：前者是**能力缺失声明**（系统里根本没有那些服务），后者是**架构约束**（由代码结构保证）。详设 §3.4.1 下方的注释**已经自己指出这一点**并建议移到 §1.3.2「不包含范围」或架构文档。⇒ 记为建议调整项（详见「执行期发现」3），**不计为门禁失败**（门禁查的是"规则要的输入有没有产生方"，这两条本就不是规则）。
- `task BR-8` / `BR-15`：写法虽偏离四类枚举，但**实查代码都能找到产出方**，不构成缺口：
  - `BR-8`（初始状态固定为 `待办`）—— 值由 `app/e2e/task/task.py:72` 的**字面量 `"待办"`** 产出；
  - `BR-15`（状态变更必须更新 `updated_at`）—— 由 `app/e2e/task/task.py:253`（`_change_status` 内 `updated_at = ?` ← `self._now()`）产出。
  - ⚠ 但 `BR-15` 的载体写的是「**平台自动注入**」，与实现（**应用自己 `_now()` 写入**）不符 ⇒ 记为文档口径偏差（详见「执行期发现」4），**不影响承载判定**。

### 2.7 每条 BR 都带 `输出`（落在哪个字段）、`生效点`（哪条链上生效）——**缺任一即不通过**

| 文件 | 三要素载体 | 行数 | 判定 |
|---|---|---|---|
| `member/应用详设.md` | §3.4.1「BR 结构化信息（供机器校验）」表 | **12 行**（`BR-1`~`BR-12`），逐行三列齐全 | ✅ |
| `task/应用详设.md` | §3.3.1「BR 结构化信息（供机器校验）」表 | **16 行**（`BR-1`~`BR-16`），逐行三列齐全 | ✅ |

- 三列**都填了内容**（不是空列）：`输出` 列用「无（拒绝）」显式声明拒绝类规则、用 `task.task_no` / `task.status` / `task.updated_at` / `member` 列表 等具体字段名写正向产物；`生效点` 列写到具体服务（`task.create`、`task.start`…），跨应用那条写 `task.create → member.get`。
- **28/28 行三要素齐全** ✅ —— 唯一边界见下条 ⚠。
- ⚠ **`member` 详设正文里还有第 13 个 BR 编号 `BR-13`（`app/e2e/member/应用详设.md:301`），它不在上面任何一张表里**：该行落在 §3.4.1 表下方的引用块**之后**，既不属于 §3.4 业务规则表（12 行），也不属于 §3.4.1 三要素表（12 行）。⇒ 机器只能读出 12 条（`br_coverage.py` 实读即报 `member BR-1`…`BR-12`）。**记为文档缺口，详见「执行期发现」1。**

---

## 三、本报告检查不到什么

**本报告只做一件事**：把 `architecture.md` 的聚合根卡与两份《应用详设.md》**逐项对照**（文档 ↔ 文档）。它**不读代码、不跑脚本、不查库**。

因此，下面这些**机械判据**才是对应维度的真正凭据。以下为**实跑结论原文**（跑于 2026-09-18，`PYTHONIOENCODING=utf-8`）：

| 工具 | 覆盖本报告的哪一项 | 实跑结论（原文） |
|---|---|---|
| `python scripts/scan_structure.py -g e2e` | 2.6 / 2.7（三要素「在不在、读不读得出来」） | `通过 8 · 失败 0 · 跳过 2` / `VERIFY_RESULT: PASS`；其中 → `漏斗：2 份详设 → 2 份可判（0 份缺三要素） → 0 份载体解析不了（SKIP）`；`✓ PASS T3 [member] BR 三要素完整 —— 备注：PRD 表格载体：另起「BR 结构化信息」表`；`✓ PASS T3 [task] BR 三要素完整`；`✓ PASS T3 判据适用性 —— 备注：2 份详设进入判据` |
| `python scripts/br_coverage.py -g e2e` | 2.7 的下游（三要素被消费的情形） | `合计 28 条 BR：输出字段被断言引用 6 · 拒绝类 19 · 中间量 1 · 判不了 0` |
| `python scripts/run_gates.py -g e2e --all` | 整组健康度 | `通过 8/8` / `VERIFY_RESULT: PASS` |

**⚠ 这三项不能替代本报告，本报告也不能替代它们：**

- **T3 只查「三要素在不在、读不读得出来」，不查「内容对不对」**。反例就在本组：`task BR-15` 的 `依赖输入` 写「平台自动注入」而实现是应用 `_now()`，T3 照样判 PASS。「这个输入系统里由谁产生」这一问，**至今只有人工审查（2.6）**，规格自己也这么写着。
- **没有工具在读本报告的对照关系**。把 `task/应用详设.md` 的 `FUNC-3` 删掉、或把 `BR-4` 从 §3.3 表里挪走，本报告会红，而上面三个工具**一个都不会报**——它们查的是代码、脚本、用例，不是「架构卡 ↔ 详设」。
- 本报告**不查**：详设与代码是否一致（见《应用编码覆盖度检查.md》+ `scan_structure` 的 S 判据）、详设与用例是否一致（见《测试用例覆盖度报告.md》）、DDL 是否真的落库（`run_gates` 的 `[static] DDL 列对账`）。
- 本报告的每一项都是**人读对照、编号逐条数过**；2.1~2.5、2.7 的数（9 / 10 / 2 / 2 / 10 / 28）均由我逐条数出，非从别处抄录。

---

## 四、执行期发现

1. **`member` 详设的 `BR-13` 是「表外孤儿行」**（`app/e2e/member/应用详设.md:301`）——
   它出现在 §3.4.1 表格**下方引用块之后**，因此：① 不属于 §3.4 业务规则表（12 行）；② 不属于 §3.4.1 三要素表（12 行）⇒ **机器判据读不到它**（`br_coverage.py` 实读 12 条）；③ 该行内容「`task.create` 可调用 `member.get` 校验成员存在性」**本身是 `task` 侧的跨应用事实，不是 `member` 的业务规则**，且与 §4.5.2（被调表）、§6 的 `AT-16` 重复。建议：并入 §4.5.2 或删除，不留业务规则区。
2. **第①步产物的落点与文件名不符规格**：规格要求聚合根卡的**固定、唯一落点**是 `app/<组>/architecture.md`（《工具链使用说明.md》第 3 步），且第 3 步门禁要求产出 `app/<组>/architecture_review.md`（业务覆盖度检查报告）。实测 `app/e2e/` 下**两者都不存在**，实际文件是 `app/e2e/architecture.md`（148 行，内容齐：§1 总表 / §2 聚合根卡 ×2 / §3 聚合关系图 / §4 构建批次）。**按文件名找产物的人会找不到**；本组也没有 `brd/`，故 2.5 以架构卡「关键属性」充作上游。内容不缺，缺的是**约定的文件名**。
3. **`member BR-11` / `BR-12` 不是业务规则**（详设自己已在 §3.4.1 下方注明）。二者无法被第④步「断言」、也无法被第③步「查实参」。建议按详设自己的建议移出业务规则表（→ §1.3.2 不包含范围 / 架构文档）。
4. **`task BR-15` 的 `依赖输入` 与实现口径不符**：载体写「—（平台自动注入）」，实现见 `task.py:253`（`_change_status` 内 `updated_at = self._now()`）—— 是**应用自己写入**，不是平台审计列注入。虽有产出方（不构成门禁缺口），但表述会误导下游。
5. **详设的对外服务签名与实现/CONVENTION §7 不一致**（第③步报告亦记一笔）：
   - `member/应用详设.md` §4.4.3 声明 `member.list(keyword: str | None = None, role: str | None = None) -> list[dict]`，而实现是 `list(keyword, role, page, size)`（`member.py:62`）且返回 `{"total","items"}`（`member.py:96`/`:107`）；
   - `task/应用详设.md` §4.4 的 `list` 同缺 `page`/`size`，且「返回」列写「任务对象列表」。
   - CONVENTION §7 明确要求带 `page`/`size` 且返回 `{"items","total"}`。**详设未同步分页形参与返回结构**（该改名是本轮 2026-09-17 的订正，代码注释见 `member.py:57-61`、`task.py:82-86`）。

---

### 修复回执（2026-09-18）

| 发现 | 处置 |
|---|---|
| 1（`member` 表外孤儿 `BR-13`） | **已删除**：其内容是 `task` 侧规则（正本 = `task` 详设 `BR-6`），`member` 只在 §3.2 描述被调用方行为。`AT-16` 的引用改为「（跨应用：`task` 详设 `BR-6`）」。原位置留了说明，防止下一个人再补一行 |
| 2（原文件名 `架构设计.md` 不符规格） | **已改名 → `app/e2e/architecture.md`**（`git mv`，保留历史），本目录 **14 处**引用同步；`design-plus/CONVENTION.md` 的目录树与说明也按新名订正。依据：平台 `agent_common.py` / `knowledge_graph.py` 的 `_ARCH_FILENAMES` 注释写明「**旧** `架构设计.md` → **新** `architecture.md`」，`前端设计.md`/`应用测试.md`/`应用编码.md` 三处规格都已写 `app/<组>/architecture.md`，`app/psc` 也早已是新名 —— 本组是最后一个用旧名的。**同批补齐了另两项**（2026-09-18，按用户决策「对齐 PSC」）：`app/e2e/brd/`（00 目录 + 01 业务背景与目标 / 02 角色与权限 / 03 成员主数据 / 04 任务管理 / 05 系统设计实现）与 `app/e2e/architecture_review.md`（业务单据 2/2、主流程 12/12、角色 4/4）。⚠ 两者的来源已在 `brd/00-目录.md` 与 review §五 如实说明：**本组 BRD 是据已冻结设计反向整理的**，故这份对账含一层同源成分 —— 能证明「设计没漏 BRD 的条目」，不能证明「BRD 穷尽了业务」 |
| 3（`member BR-11/BR-12` 不是业务规则） | **未动**：详设自己已注明，且 `br_coverage` 已把它们归一为「显式填不出」（不算未覆盖）。移表是文档结构决定，列在 `测试用例.md` §6 待确认 1 |
| 4（`task BR-15` 写「平台自动注入」） | **已订正**：实为 `_change_status` 显式写 `updated_at = _now()`（`task.py:253`）—— 产出方写错了，已改成「**本表**：`_change_status` 显式写…」 |
| 5（`list` 签名/返回未同步） | **已回填**：`member` §4.4.3 补 `page`/`size` + `{"total","items"}` 返回形状与异常清单；`task` §4.4 的 `list` 行同批补齐（含"不分页也返回同形字典"）。另补 `member` §4.3.1 索引需求（此前索引只存在于 `schema.sql`、详设无上游）与 §4.3 第 5 条（`updated_at` 实为平台注入列，原写"本表不包含"与实库不符） |

---

## 五、总体结论

| 门禁项（第 4 步 7 条） | 覆盖度 | 判定 |
|---|---|---|
| 对外操作 → US/FUNC | 9/9 = 100% | ✅ 通过 |
| 关键不变量 → BR | 10/10 = 100% | ✅ 通过 |
| 状态机 → 「状态机」节 | 2/2 应用 = 100% | ✅ 通过 |
| 跨应用调用 → 外部依赖节 | 2/2 应用 = 100%（1/1 条调用） | ✅ 通过 |
| 单据字段 → §4 字段表 | 10/10 = 100% | ✅ 通过 |
| 每条 BR 的 `依赖输入` 逐项有承载（人工） | 26/28 明确承载；2 条非规则（已自注）；2 条写法偏离枚举但实查有产出方 | ⚠ 通过（附建议） |
| 每条 BR 带 `输出` + `生效点`（机器可读） | 28/28 行齐全（`scan_structure` T3 判 PASS）；**另有 1 个编号 `BR-13` 落在表外、机器读不到** | ⚠ 通过（1 处文档缺口） |
| **机械判据复核** | `scan_structure -g e2e` 通过 8/失败 0 · `br_coverage` 判不了 0 · `run_gates --all` 8/8 | ✅ 全部 PASS |
| **总体** | **门禁可校验面 100%** | **✅ 通过，可进入第③步（应用编码）**；附 1 处文档缺口（`member BR-13`）与 4 项建议（①②③④⑤ 见上节），均不影响门禁判定 |

> 产物：2 份《应用详设.md》（合计 1025 行），覆盖 `architecture.md` 的全部 2 个聚合根。

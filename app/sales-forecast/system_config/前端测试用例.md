# system_config 前端测试用例

> 逐应用 · 第⑧步产出 | 对应前端详设 PAGE_META.key = system_config

## TC-SC-01 本页渲染
- 页面: `/view/sales-forecast/#system_config`
- 操作: admin 登录后访问
- 断言:
  1. Tab 栏存在，含 5 个标签页：参数/模板库/类比库/信任折扣/外部数据源
  2. 默认选中"参数" tab，对应列表区或配置区存在
  3. 非看板页 `.kpi` 数量 == 0（防粘滞——确保不是 dashboard 残留）

## TC-SC-02 Tab 切换
- 前置: 页面已渲染
- 操作: 依次点击各 tab（参数 → 模板库 → 类比库 → 信任折扣 → 外部数据源）
- 断言:
  1. 每次点击 tab 高亮切换
  2. 内容区刷新为对应 tab 的配置项
  3. 无报错或空白

## TC-SC-03 参数 tab —— 列表渲染
- 前置: 选中"参数" tab
- 操作: 查看参数列表
- 断言:
  1. 列表含参数编码、参数名称、参数值、描述等列
  2. 搜索筛选功能正常

## TC-SC-04 参数 tab —— 行内编辑
- 前置: 选中"参数" tab，列表已渲染
- 操作: 双击某参数行或点击编辑按钮进入行内编辑模式
- 断言:
  1. 参数值变为可编辑 input
  2. 修改值后点击保存（回车或确认按钮）
  3. toast 提示 "参数已更新"
  4. 列表刷新显示新值

## TC-SC-05 模板库 tab —— 列表渲染
- 前置: 选中"模板库" tab
- 操作: 查看模板列表
- 断言:
  1. 列表含模板编码、模板名称、适用场景、状态等列
  2. 可点击查看模板详情

## TC-SC-06 类比库 tab —— 列表渲染
- 前置: 选中"类比库" tab
- 操作: 查看类比库列表
- 断言:
  1. 列表含类比编码、源零件、目标零件、类比系数、状态等列
  2. 搜索筛选功能正常

## TC-SC-07 0 console error
- 断言: 本页整个会话期间（遍历所有 tab 并做一次行内编辑）浏览器 console 无 error（favicon 除外），HTTP 无 >=400（业务校验抛 FdeError 属正常 200）

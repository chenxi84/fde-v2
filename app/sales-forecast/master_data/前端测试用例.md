# master_data 前端测试用例

> 逐应用 · 第⑧步产出 | 对应前端详设 PAGE_META.key = master_data

## TC-MD-01 本页渲染
- 页面: `/view/sales-forecast/#master_data`
- 操作: admin 登录后访问
- 断言:
  1. Tab 栏存在，含 5 个标签页：客户/物料/车型/用户/日历
  2. 默认选中"客户" tab，对应列表区 `.card .tbl` 存在
  3. 非看板页 `.kpi` 数量 == 0（防粘滞——确保不是 dashboard 残留）

## TC-MD-02 Tab 切换
- 前置: 页面已渲染
- 操作: 依次点击各 tab（客户 → 物料 → 车型 → 用户 → 日历）
- 断言:
  1. 每次点击 tab 高亮切换
  2. 列表区刷新为对应 tab 的列和数据
  3. 每个 tab 的搜索栏/筛选器随 tab 变化

## TC-MD-03 客户 tab —— 列表与操作
- 前置: 选中"客户" tab
- 操作: 查看列表列，若有关联的新建按钮则点击
- 断言:
  1. 列表含客户编码、客户名称、OEM、状态等列
  2. 如有新建按钮，点击后表单展开
  3. 搜索/筛选功能正常

## TC-MD-04 物料 tab —— 列表与操作
- 前置: 选中"物料" tab
- 操作: 查看列表列
- 断言:
  1. 列表含零件号、零件名称、零件种类、单位等列
  2. 搜索输入框可搜索零件号或名称

## TC-MD-05 车型 tab —— 列表与操作
- 前置: 选中"车型" tab
- 操作: 查看列表列
- 断言:
  1. 列表含车型编码、车型名称、平台、SOP、EOP 等列
  2. 搜索筛选功能正常

## TC-MD-06 日历 tab —— 列表与操作
- 前置: 选中"日历" tab
- 操作: 查看列表列
- 断言:
  1. 列表含年度、期间、开始日期、结束日期、工作日天数等列
  2. 数据按年度-期间排序

## TC-MD-07 0 console error
- 断言: 本页整个会话期间（遍历所有 tab）浏览器 console 无 error（favicon 除外），HTTP 无 >=400（业务校验抛 FdeError 属正常 200）

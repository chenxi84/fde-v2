# 前端详设：牛鞭修正处理表（bullwhip_correction）

> PAGE_META.key = `bullwhip_correction` | order = `120`

## PAGE_META
- key: `bullwhip_correction`, name: `牛鞭修正`, ic: `trending-up`, title: `牛鞭修正处理`, crumb: `D08 终端口径锚定`, order: `120`

## 区块构成

### 列表
- 列: bw_no, part_no, period, tier, derived_qty, end_qty, amp_factor, amp_verdict, status, checker, chk_date
- 搜索: part_no（文本输入）, period（日期选择）, tier（枚举多选：Tier1直供/Tier2/Tier3）, status（多选枚举：待处理/维持/已修正/已确认）, amp_verdict（枚举多选：容忍内·维持/超阈·修正）
- 操作:
  - 待处理 → [牛鞭修正] [维持] [查看详情]
  - 维持 → [确认] [查看详情]
  - 已修正 → [确认] [查看详情]
  - 已确认 → [查看详情]

### 详情模态
- 字段分组:
  - 处理单标识: bw_no, part_no, period, tier, status
  - 派生 vs 终端: derived_qty（下游订单/派生需求）, end_qty（终端需求/消耗口径）, end_source（终端来源）
  - 放大系数: amp_factor（=derived/end，系统计算）, amp_verdict（容忍内·维持/超阈·修正）
  - 修正动作: corr_action（修正机制+说明）, final_qty（终端口径最终值）
  - 操作人: checker, chk_date
- 子账本: 无
- 页脚操作:
  - 待处理 → [牛鞭修正] [维持]
  - 维持 → [确认]
  - 已修正 → [确认]
  - 已确认 → 只读

### 表单（牛鞭修正）
- 可写字段:
  - 必填: corr_action（修正动作：机制菜单+说明）, final_qty（修正后预测/终端口径最终值）
  - 不可写: part_no, period, tier, derived_qty, end_qty, amp_factor, amp_verdict（只读展示）
  - 折叠: —（无折叠字段）
- 必填项: corr_action, final_qty
- 可选/折叠: —

### 维持确认子表单
- 可写字段:
  - 必填: —（仅确认操作，无额外输入）
  - 不可写: bw_no, part_no, period, amp_factor, amp_verdict（只读展示）
- 必填项: —（点击确认按钮即可）
- 可选/折叠: —

## svc 字面量清单
- svc("bullwhip_correction", "list") — 处理单列表（按零件/期间/层级/状态筛选）
- svc("bullwhip_correction", "get") — 处理单详情
- svc("bullwhip_correction", "create") — 新建处理单（系统自动计算放大系数）
- svc("bullwhip_correction", "set_correction") — 执行修正（选机制+登记依据）
- svc("bullwhip_correction", "maintain") — 容忍内维持
- svc("bullwhip_correction", "confirm") — 确认（转发布）

## 验收关注点
- 新建按钮存在（系统自动创建，前端视情况展示创建入口）
- 非看板页 .kpi==0
- 列表列 ⊆ 详情字段集（列表字段均在详情模态中出现）
- 搜索控件与 list 参数一一对应（part_no/period/tier/status/amp_verdict）
- 表单字段 = 写服务接受参数（set_correction 参数与修正表单一致）
- amp_factor 系统计算，前端只读展示
- 超阈（amp_verdict="超阈·修正"）时前端红色预警提示
- Tier1 直供+ amp_factor ≤ θ_amp 时前端提示"简化确认"可直接维持
- corr_action 非空校验（禁止无机制纯减数）
- amp_verdict="超阈·修正"时 corr_action 必填

# 产销协同应用组 · 前端测试用例覆盖度检查报告（第⑧步）

> 产出时间：2026-08-14
> 依据：15 份前端详设 + app/psc/_contracts.md（最终冻结）+ design-plus/前端测试.md + view-convention/pitfalls.md
> 检查对象：15 份 `app/psc/<应用>/前端测试用例.md` + 1 份 `app/psc/前端测试用例.md`（组级补充）
> 规格：design-plus/前端测试.md

---

## 一、文件完整性

15 逐应用 + 1 组级补充 = 16 文件全部产出 ✅

| 应用 | 用例数 | 应用 | 用例数 |
|------|-------|------|-------|
| md_customer | 9 | sales_forecast | 10 |
| md_material | 10 | inventory_strategy | 12 |
| md_project | 16 | demand | 16 |
| md_project_part | 16 | master_plan | 7 |
| md_breakpoint | 11 | inventory_projection | 13 |
| md_part_replace | 13 | demand_pool | 17 |
| md_monthly_version | 15 | strategy_fitting | 13 |
| attainment | 8 | 组级补充 | 5 |

---

## 二、完成门禁逐项核对

### 2.1 逐应用六段结构（§0–§4 + §6）

- 抽查 md_customer/md_material/demand/demand_pool/sales_forecast：§0 数据字典 / §1 渲染（防粘滞 .kpi==0）/ §2 列表 / §3 模态 / §4 表单（可写应用）或豁免（自动参考创建）/ §6 零报错 **全部存在**。
- 自动参考创建 4 应用（attainment/inventory_strategy/inventory_projection/demand_pool）§4 按详设豁免并注明理由（calc 计算入口 / 状态机操作 / 系统触发）。✅

### 2.2 组级补充（§1 壳+菜单序+dashboard + §5 受限三连 + §6）

- VT-SHELL-01：壳启动 + 菜单 16 项按 `PAGE_META.order` 升序（key/order/中文名逐行表）。
- VT-DASH-01：看板页渲染（KPI/主链管道/待办三区块，`.kpi`>0）。
- VT-PERM-01/02/03：受限三连——菜单收敛、直访无授权 hash 回落菜单首项、隐式放行有数据。
- **受限授权清单逐字**：`["psc:dashboard", "psc:md_customer", "psc:md_monthly_version", "_platform:agent"]`。✅

### 2.3 五要素 + 造数服务名以契约为纲

- 抽查组级补充与逐应用文件：每条用例含页面/前置造数/操作/断言/选择器注意五要素。
- 造数服务名（`md_customer.create`/`md_material.create`/`attainment.upsert`/`demand_pool.create` 等）均可在 `_contracts.md` 找到；受限角色/用户经 `fde_platform.users` 平台模块播种（已注明，非浏览器 REST）。✅
- 断言可机器校验（文本含/计数/集合相等/`.kpi`==0/0 报错）。✅

### 2.4 选择器注意标注

- 各文件按 pitfalls 标注：`.kpi`==0 防粘滞（#2）、`:visible` 限定（#14）、`.modal-bd.first`（#15）、`wait_modal` 等 `.loadbox` 消失（#16）、精确单号 `has_text`（#17）、造数走真实 REST（#18）。✅

---

## 三、执行期发现并纠偏

| # | 问题 | 纠偏 |
|---|------|------|
| G1 | 组级补充的看板菜单名——子代理发现我 prompt 里写的「首页看板」是 e2e/crm 看板名，psc 看板真实名是「产销协同看板」（`view/psc/dashboard.js` 的 PAGE_META.name） | 菜单首项与回落断言均已按「产销协同看板」写 |

---

## 四、总体结论

| 维度 | 覆盖度 | 判定 |
|------|--------|------|
| 文件完整性（16 文件） | 16/16 | ✅ 通过 |
| 逐应用六段结构（§0–§4+§6） | 100% | ✅ 通过 |
| 组级补充（壳/菜单序/dashboard/受限三连/0报错） | 100% | ✅ 通过 |
| 五要素 + 造数服务名契约为纲 + 断言可机器校验 | 100% | ✅ 通过 |
| 受限授权清单逐字 + 菜单序与 PAGE_META 一致 | 100% | ✅ 通过 |
| **总体** | **100%** | **✅ 通过，可进入第⑨步（前端测试执行）** |

> 注：本步只生成用例（16 份结构化 Markdown），不运行；第⑨步翻译成 playwright 脚本（逐应用 `verify_view_psc_<应用>.py` + 组级 `verify_view_psc.py`）并 0 报错跑通。

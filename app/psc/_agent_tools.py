"""psc 组：哪些服务**不给智能体调**（组级下沉声明，与 _roles.py 同一套约定）。

为什么要有这份声明
------------------
AgentScope 会把应用的每个公共服务都变成一个工具，而工具数直接决定选择准确率——
外部证据（arXiv:2605.24660）显示单次呈现超过 ~60 个工具即进入塌陷区，且**多余的
候选会主动伤害**准确率。所以工具面要主动收：agent 永远不该调的服务不该出现在它的
工具表里。

用黑名单而不是白名单：白名单要为 180 个服务逐一表态，加应用就要同步改，维护不动；
而「agent 不该碰」的服务是少数、稳定、有明确特征（下面三类）。

三类该摘的服务
--------------
1. **内部回填** —— 由别的应用在流程中调用。agent 绕过流程直接调是错的，
   比如直接改物料拟合参数，会让 `strategy_fitting` 的「建议→复核→生效」链路失去意义。
2. **外部回执** —— ERP / 周边系统回调进来的入口，不是 agent 发起的动作。
3. **被 batch 版取代的单行版** —— agent 该用批量接口；逐行调既慢，又容易只改一半。

**只影响 agent 的工具表，不影响 `self.fde.call` 的跨应用调用**——
应用之间该怎么调还怎么调，这里管的是「模型能不能看到」。

加新条目
--------
服务名写**公共方法名**（不带应用前缀）。摘错了会让 agent 少一条路，
不确定的宁可不摘——塌陷是概率问题，功能缺失是确定问题。
"""
HIDDEN_FROM_AGENT = {
    # ── 内部回填：由其他应用在流程中调用 ──
    "psc/md_material": [
        "set_fit_params",        # 由 strategy_fitting.approve 回填，agent 直接改会跳过复核
    ],
    # 注：master_plan.import_plan **不在**隐藏列表里。它曾是「线下产能平衡结果导回，
    # 属人工/外部动作」被摘掉，但读完 README 发现它是主计划**唯一的数据入口**——
    # 摘掉等于 agent 完全无法写主计划；而「人工动作」不等于「agent 不能代劳」，
    # 用户说「把这份平衡表导进去」是完全合理的请求。

    # ── 外部回执：ERP / 周边系统回调入口 ──
    "psc/demand_pool": [
        "on_workorder_started",  # ERP 回传工单开工
        "on_inbound",            # ERP 回传入库
    ],

    # ── ERP 冗余回写：数据该由同步服务整体拉，不该逐条 upsert ──
    "psc/sales_history": [
        "upsert",                # 单条回写
        "attach_forecast",       # 预测侧推送，由 sales_forecast 调
        "sync_forecast",         # 批量回填 forecast_qty，由 attainment 调
    ],
    "psc/attainment": [
        "upsert",                # ERP 统计回写
    ],

    # ── 被 batch 版取代的单行版 / 内部投影 ──
    "psc/sales_forecast": [
        "calc_baseline",              # agent 用 calc_baseline_batch
        "decide",                     # agent 用 decide_batch
        "fill_customer",              # 单行填数；agent 用 import_orig_qty（内部仍会走它）
        "customer_forecast_history",  # 供 attainment 算达成率的投影，不是 agent 的动作
    ],
    "psc/inventory_projection": [
        "refresh",               # agent 用 refresh_batch
    ],
}


# ── 组描述提示：模型靠它决定「开哪一组」 ─────────────────────────
#
# 为什么需要这一层：组描述默认取前端 PAGE_META 的中文名与 crumb，而相邻应用的 crumb
# 高度同构（都是「计划/排产专家域：xxx」这种）。实测 A/B 评测里**最大的新增失败模式
# 就是组选错**——`demand_pool` 被选成 `demand`、`md_project` 被选成 `master_plan`。
# 所以每组要写清「什么时候用它」**以及「什么时候该用隔壁那个」**。
GROUP_HINTS = {
    "psc/md_customer":
        "客户档案（结算方式、线边库存天数、调拨提前期）。"
        "查客户「报得准不准」不在这里，在『达成率与置信度』",
    "psc/md_material":
        "物料主数据与代际谱系：基础参数、前序料号（上一代）、统一历史链。"
        "查「这个料的上一代是什么」「把历史并起来看」用它",
    "psc/md_project":
        "车型项目的生命周期：阶段（进行中/SOP/EOP）、SOP/EOP 日期、负责人。"
        "查「项目现在什么阶段」用它，不是『主计划』",
    "psc/md_project_part":
        "项目与零件的用量映射（单车用量、份额）。"
        "查「这个项目挂哪些零件」用它，不是『物料主数据』",
    "psc/md_breakpoint":
        "新旧料切换的断点关系、向上追溯的原物料号链、切换时间。"
        "查「这个新料的断点链/上一代」用它",
    "psc/md_part_replace":
        "零件替换关系（原件→替换件）与 ECN 依据。"
        "查「有没有替换件」用它，不是『断点基础数据』",
    "psc/md_monthly_version":
        "月度版本本身：版本号、锚定期间、锁定状态（草稿/发布/冻结）、当前活跃版本。"
        "查「活跃版本是哪个」用它",
    "psc/attainment":
        "客户×物料的预测达成率与偏差（MAPE/bias）——判断「客户报的预测准不准、"
        "习惯性报高还是报低」用它。不要拿『客户主数据』或『销售预测』去答这个问题",
    "psc/sales_history":
        "历史台账：物料×客户×月度的**已发生**需求，以及按期间升序的历史序列。"
        "查「过去卖了多少」用它",
    "psc/sales_forecast":
        "月度版本内的销售预测清单：客户填报、调整后需求、基线、决策、汇总，"
        "以及客户原始预测导入。查「这个版本预测了什么」「把客户预测导进来」用它",
    "psc/strategy_fitting":
        "策略拟合：滚动回测选最优预测方法与参数、MASE、预测值与预测区间，"
        "以及复核/否决/回滚。查「该用哪个模型」「拟合结果如何」用它",
    "psc/demand":
        "毛需求与净需求的合成、发布与查询。查「这个版本要多少量（需求）」用它——"
        "**不是『需求池』（那里是补库单），也不是『主计划』（那里是已排定产量）**",
    "psc/demand_pool":
        "补库单：待下达/已下达/生产中/已完成/已取消的补库单查询与流转。"
        "查「有哪些补库单」「补了几单」用它，不是『毛需求与净需求』",
    "psc/master_plan":
        "主计划：线下产能平衡后导回的**已排定产量**与最新入库日期。"
        "查「计划排产了多少、什么时候到货」用它；查「需求是多少」用『毛需求与净需求』",
    "psc/inventory_strategy":
        "库存策略：三层水位（最低/安全/组批）、对冲工具（库存对冲/速度对冲）、"
        "服务系数。查「水位该定多少、现在水位线是多少」用它，不是『库存推移表』",
    "psc/inventory_projection":
        "库存推移表：逐日推演未来 3 个月的库存水位，带缺货/击穿/呆滞/超储预警。"
        "查「未来哪天会缺货」用它，**不是『库存策略』（那里是水位线本身）**",
    "psc/outbound_plan":
        "出库计划：登记、编辑延期、关闭到期、删除。查「有哪些出库计划」"
        "或要新增/延期一条，用它",
}

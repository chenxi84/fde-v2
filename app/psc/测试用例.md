# 应用组 `psc` 测试用例（产销协同管理）

> 产出技能：测试用例生成（第④步，规格 design-plus/应用测试.md）。
> 输入：app/psc/architecture.md + 15 份应用详设 + app/psc/_contracts.md。
> 本文件为整组联通测试用例，含自洽测试数据；生成与执行分离（第⑤步翻译成 verify_chain_psc.py 并跑通）。

---

## §0 测试数据字典（冻结）

| 标识 | 类型 | 含义 | 关键值 |
|---|---|---|---|
| C001 | 客户 | 客户A | settle_mode=寄售, credit_code=91110000710931000M, line_stock_days=0, transfer_lead_days=3 |
| C002 | 客户 | 客户B | settle_mode=现售, credit_code=91350100M000100C4G, line_stock_days=2, transfer_lead_days=5 |
| M1 | 物料 | 物料A（正常） | unit_value=100, value_class=低, change_cost=5000, prod_days=10, logistics_days=5, change_risk=低, service_level=0.95, batch_window=28, base_method=移动平均, base_params={"window":6} |
| M2 | 物料 | 物料B（正常） | 同上（多客户通用件场景） |
| M3 | 物料 | 旧件（断点原物料） | 正常，base_method=移动平均 |
| M4 | 物料 | 新件（断点新物料） | 正常，base_method=移动平均 |
| PRJ-1 | 项目 | 项目1 | 进行中, sop_date=2026-01-01, eop_date=2027-12-31, owner=S001 |
| V202608 | 月度版本 | 2026年8月版本 | anchor_period=2026-08, opening_date=2026-08-01 |
| FIT-202608 | 拟合版本 | 2026年8月拟合 | — |

> 说明：历史干净需求（`_load_sales_history`）、ERP 库存/在途/未发（`_load_*`）为适配器 stub，当前返回空/0 —— 基线 `base_qty=None`、库存水位 A/C/B=0、净需求扣减项为 0。用例期望据此设计（见 §4 待确认）。

---

## §1 主数据准备

- **TC-DM-01 创建客户 C001**
  - 步骤: `md_customer.create(customer_no=C001, customer_name=客户A, settle_mode=寄售, credit_code=91110000710931000M, line_stock_days=0, transfer_lead_days=3)`
  - 期望: 返回 `customer_no=C001`；`md_customer.get(customer_no=C001)` 返回 `line_stock_days=0`

- **TC-DM-02 创建客户 C002**
  - 步骤: `md_customer.create(customer_no=C002, customer_name=客户B, settle_mode=现售, credit_code=91350100M000100C4G, line_stock_days=2, transfer_lead_days=5)`
  - 期望: 返回 `customer_no=C002`

- **TC-DM-03 创建物料 M1（含库存策略参数）**
  - 步骤: `md_material.create(material_no=M1, material_name=物料A, status=正常, unit_value=100, value_class=低, change_cost=5000, prod_days=10, logistics_days=5, change_risk=低, service_level=0.95, batch_window=28, base_method=移动平均, base_params={"window":6})`
  - 期望: 返回 `material_no=M1`；`md_material.get(material_no=M1)` 返回 `base_method=移动平均`、`service_level=0.95`

- **TC-DM-04 创建物料 M2（多客户通用件）**
  - 步骤: `md_material.create(material_no=M2, material_name=物料B, base_method=移动平均, base_params={"window":6})`
  - 期望: 返回 `material_no=M2`

- **TC-DM-05 创建断点旧件 M3**
  - 步骤: `md_material.create(material_no=M3, material_name=旧件, base_method=移动平均, base_params={"window":6})`
  - 期望: 返回 `material_no=M3`

- **TC-DM-06 创建断点新件 M4**
  - 步骤: `md_material.create(material_no=M4, material_name=新件, base_method=移动平均, base_params={"window":6})`
  - 期望: 返回 `material_no=M4`

- **TC-DM-07 创建项目 PRJ-1**
  - 步骤: `md_project.create(project_no=PRJ-1, project_name=项目1, owner=S001, stage=进行中, sop_date=2026-01-01, eop_date=2027-12-31)`
  - 期望: 返回 `project_no=PRJ-1`、`stage=进行中`

- **TC-DM-08 创建项目零件映射（PRJ-1 × M1）**
  - 步骤: `md_project_part.create(project_no=PRJ-1, material_no=M1, veh_model=车型X, usage=1, share=1.0)`
  - 期望: 返回 `project_no=PRJ-1, material_no=M1`

- **TC-DM-09 创建月度版本 V202608**
  - 步骤: `md_monthly_version.create(version_no=V202608, anchor_period=2026-08, opening_date=2026-08-01)`
  - 期望: 返回 `version_no=V202608`、`lock_status=草稿`

- **TC-DM-10 写入客户达成率（MAPE/bias）**
  - 步骤: `attainment.upsert(customer_no=C001, material_no=M1, mape=0.10, bias=0.05)`
  - 期望: `attainment.get(customer_no=C001, material_no=M1)` 返回 `mape=0.10`、`bias=0.05`

---

## §2 主业务链（happy path）

- **TC-MC-01 开启月度版本**
  - 步骤: `sales_forecast.open_version(version_no=V202608)`
  - 期望: 返回处理表行信息；`sales_forecast.list(version_no=V202608)` 返回 `total >= 1`（正常物料 × 历史采购客户 → 拆 N+1/N+2/N+3）
  - 调用链验证: open_version → `md_material.list(status=正常)` + `md_monthly_version.get` + `_load_sales_history`

- **TC-MC-02 填客户预测（N+1/N+2/N+3）**
  - 数据: C001 × M1 三个滚动月度
  - 步骤:
    1. `sales_forecast.fill_customer(version_no=V202608, material_no=M1, customer_no=C001, rolling_month=N+1, orig_qty=1000)`
    2. 同 N+2、N+3 各 orig_qty=1000
  - 期望: `sales_forecast.get(version_no=V202608, material_no=M1, customer_no=C001, rolling_month=N+1)` 返回 `orig_qty=1000`、`adj_qty=950`（= 1000 × (1−0.05)）
  - 调用链验证: fill_customer → `md_material.get` + `md_customer.get` 校验存在

- **TC-MC-03 基线计算（含断点追溯前置）**
  - 步骤: `sales_forecast.calc_baseline(version_no=V202608, material_no=M1, customer_no=C001, rolling_month=N+1)`
  - 期望: 返回处理表行，`base_method=移动平均`、`base_qty=None`（stub 历史为空）
  - 调用链验证: calc_baseline → `md_breakpoint.trace` + `md_material.get` + `_load_sales_history`

- **TC-MC-04 异常决策（基线未计算 → 取客户调整后值）**
  - 步骤: `sales_forecast.decide(version_no=V202608, material_no=M1, customer_no=C001, rolling_month=N+1)`
  - 期望: 返回 `abnormal_flag=false`、`final_qty=950`（base_event_qty=None → 取 adj_qty）
  - 调用链验证: decide → `attainment.get(customer_no=C001, material_no=M1)` 取 MAPE

- **TC-MC-05 物料预测汇总**
  - 步骤: `sales_forecast.summarize(version_no=V202608)`
  - 期望: 返回 `summary_rows >= 1`；`sales_forecast.get_summary(version_no=V202608, material_no=M1)` 返回 3 行（N+1/N+2/N+3），每行 `final_qty_sum=950`（汇总按物料×滚动月度逐行，非合计值）

- **TC-MC-06 计算库存策略（三层水位）**
  - 步骤: `inventory_strategy.calc(version_no=V202608, material_no=M1, customer_no=C001)`
  - 期望: `inventory_strategy.get(version_no=V202608, material_no=M1)` 返回 `hedge_tool`、`min_level`、`safety_level`、`batch_level` 字段（stub 历史空 → 日需求=0 → 三层水位=0）
  - 调用链验证: calc → `md_material.get` + `md_customer.get` + `_load_sales_history`

- **TC-MC-07 取三层水位（供毛需求）**
  - 步骤: `inventory_strategy.get_water_level(version_no=V202608, material_no=M1)`
  - 期望: 返回含 `min_level`/`safety_level`/`batch_level` 字段

- **TC-MC-08 合成毛需求**
  - 步骤: `demand.build_gross(version_no=V202608)`
  - 期望: 返回 `material_count >= 1`；`demand.get(version_no=V202608, material_no=M1, rolling_month=N+1)` 返回 `gross_qty=950`（= forecast 950 + 水位 0）
  - 调用链验证: build_gross → `sales_forecast.get_summary` + `inventory_strategy.get_water_level` + `md_part_replace.list` + `md_breakpoint.list`

- **TC-MC-09 发布毛需求（冻结）**
  - 步骤: `demand.publish(version_no=V202608)`
  - 期望: 返回 `message` 含"发布成功"；`md_monthly_version.get(version_no=V202608)` 返回 `lock_status=发布（锁定）`
  - 调用链验证: publish → `md_monthly_version.publish`

- **TC-MC-10 净需求运算（冻结版本）**
  - 步骤: `demand.calc_net(version_no=V202608)`
  - 期望: 返回 `row_count >= 1`；`demand.get(...)` 返回 `net_qty=950`（gross 950 + 未发 0 − 库存 0 − 在途 0）；`md_monthly_version.get(...)` 返回 `lock_status=冻结`
  - 调用链验证: calc_net → `md_monthly_version.freeze` + `_load_open_order`/`_load_inventory`/`_load_in_transit`

- **TC-MC-11 导入主计划（线下产能平衡结果导回）**
  - 步骤: `master_plan.import_plan(version_no=V202608, rows=[{material_no:M1, rolling_month:N+1, plan_qty:950, latest_inbound_date:2026-09-30}])`
  - 期望: 返回 `plan_version=1`、`success=1`；`master_plan.get_latest(version_no=V202608, material_no=M1)` 返回 `plan_qty=950`
  - 调用链验证: import_plan → `demand.export_net` 导出净需求

- **TC-MC-12 库存推移表逐日推演**
  - 步骤: `inventory_projection.refresh(material_no=M1, biz_date=2026-08-15, opening_stock=0)`
  - 期望: 返回推演结果；`inventory_projection.get(material_no=M1, biz_date=2026-08-15)` 返回 `balance` 字段
  - 调用链验证: refresh → `master_plan.get_latest` + `inventory_strategy.get_water_level` + `_load_inventory`/`_load_in_transit`/`_load_transfer_order`

- **TC-MC-13 预警扫描（对照水位线）**
  - 步骤: `inventory_projection.scan_alert(material_no=M1, version_no=V202608)`
  - 期望: 返回 alert 结果；`inventory_projection.list(material_no=M1)` 各日 `alert_type` 已回填。注：缺货判定为 `balance < 0`（stub 下 balance=0、水位 A=0，不击穿不触发补库；三类补库的确定性覆盖由 §TC-ERR-20~22 直接调 `demand_pool.create` 承担）
  - 调用链验证: scan_alert → `inventory_strategy.get_water_level`（`demand_pool.create` 触发依赖 balance<0，见 §4 Q2）

- **TC-MC-14 补库单下达生产（ERP 出向）**
  - 前置: 取 §TC-MC-13 或 §TC-ERR-24 生成的补库单号 `RP001`（缺货补库）
  - 步骤: `demand_pool.release(replenish_no=RP001, promised_inbound=2026-08-20)`
  - 期望: `demand_pool.get(replenish_no=RP001)` 返回 `status=已下达`
  - 回执模拟点: 出向 `_dispatch_to_erp` 走 stub，无需模拟

- **TC-MC-15 ERP 回传工单开工（异步回执模拟）**
  - 步骤: `demand_pool.on_workorder_started(replenish_no=RP001)`
  - 回执模拟: 主动调用 `on_workorder_started` 模拟 ERP 开工回执（成功路）
  - 期望: `demand_pool.get(replenish_no=RP001)` 返回 `status=生产中`

- **TC-MC-16 ERP 回传入库（异步回执模拟，终态）**
  - 步骤: `demand_pool.on_inbound(replenish_no=RP001)`
  - 回执模拟: 主动调用 `on_inbound` 模拟 ERP 入库回执（成功路）
  - 期望: `demand_pool.get(replenish_no=RP001)` 返回 `status=已完成`

- **TC-MC-17 策略拟合（预测拟合 + 库存拟合）**
  - 步骤: `strategy_fitting.run(material_no=M1, fit_version=FIT-202608)`
  - 期望: 返回拟合结果；`strategy_fitting.get(fit_version=FIT-202608, material_no=M1)` 返回 `status=待复核`
  - 调用链验证: run → `md_material.get` + `_load_sales_history`

- **TC-MC-18 拟合复核通过（回填物料主数据）**
  - 步骤: `strategy_fitting.approve(fit_version=FIT-202608, material_no=M1)`
  - 期望: 返回 `status=已生效`；`md_material.get(material_no=M1)` 参数已回填（`base_method`/`base_params`/`batch_window`/`service_level` 按拟合结果更新，带 fit_version 版本）
  - 调用链验证: approve → `md_material.set_fit_params`

---

## §3 分支 / 异常用例

### 3.1 主数据校验分支

- **TC-ERR-01 客户编码重复拦截**
  - 步骤: `md_customer.create(customer_no=C001, customer_name=重复客户)`
  - 期望: 抛 `FdeError` 含『已存在』或『重复』

- **TC-ERR-02 物料号重复拦截**
  - 步骤: `md_material.create(material_no=M1, material_name=重复物料)`
  - 期望: 抛 `FdeError` 含『已存在』

- **TC-ERR-03 拟合参数与基线方法不匹配拦截**
  - 步骤: `md_material.set_fit_params(material_no=M1, base_method=移动平均, base_params={"alpha":0.3}, batch_window=28, service_level=0.95, fit_version=FIT-202608)`
  - 期望: 抛 `FdeError` 含『不匹配』或『window』（移动平均缺 window 键）

- **TC-ERR-04 项目阶段跳级拦截**
  - 步骤: `md_project.update(project_no=PRJ-1, stage=EOP)`（进行中 → EOP 跳级，须经 SOP）
  - 期望: 抛 `FdeError` 含『阶段』（单向流转禁止跳级）

- **TC-ERR-05 断点原物料=新物料拦截**
  - 步骤: `md_breakpoint.create(customer_no=C001, old_material_no=M3, new_material_no=M3, switch_time=2026-09-01)`
  - 期望: 抛 `FdeError` 含『原』『新』或『相同』

- **TC-ERR-06 替换关系原=替换拦截**
  - 步骤: `md_part_replace.create(old_material_no=M1, new_material_no=M1)`
  - 期望: 抛 `FdeError` 含『原』『新』或『相同』

- **TC-ERR-07 项目零件映射引用不存在的项目**
  - 步骤: `md_project_part.create(project_no=NOTEXIST, material_no=M1, veh_model=X, usage=1, share=1.0)`
  - 期望: 抛 `FdeError` 含『项目』（md_project.get 校验失败）
  - 调用链验证: create → `md_project.get`

### 3.2 月度版本状态机分支

- **TC-ERR-08 非草稿版本重复发布拦截**
  - 前置: V202608 已在主链发布
  - 步骤: `md_monthly_version.publish(version_no=V202608)`
  - 期望: 抛 `FdeError` 含『草稿』

- **TC-ERR-09 草稿版本直接冻结拦截**
  - 前置: 新建草稿版本 V202609
  - 步骤: `md_monthly_version.freeze(version_no=V202609)`
  - 期望: 抛 `FdeError` 含『发布』（仅发布态可冻结）

- **TC-ERR-10 已冻结版本再冻结拦截（不可逆）**
  - 前置: V202608 已冻结
  - 步骤: `md_monthly_version.freeze(version_no=V202608)`
  - 期望: 抛 `FdeError` 含『不可逆』或『冻结』

### 3.3 销售预测分支

- **TC-ERR-11 已发布版本填预测拦截**
  - 前置: V202608 已发布（锁定）
  - 步骤: `sales_forecast.fill_customer(version_no=V202608, material_no=M2, customer_no=C002, rolling_month=N+1, orig_qty=100)`
  - 期望: 抛 `FdeError` 含『草稿』或『锁定』（_require_draft 拦截）

- **TC-ERR-12 非异常行人工填最终预测拦截**
  - 前置: M2 的 decide 结果为非异常（stub 基线 None → 自动填）
  - 步骤: `sales_forecast.set_final(version_no=V202608, material_no=M2, customer_no=C002, rolling_month=N+1, final_qty=999)`
  - 期望: 抛 `FdeError` 含『异常』（非异常行拒绝人工覆盖）

### 3.4 毛需求/净需求分支

- **TC-ERR-13 已发布版本合成毛需求拦截**
  - 前置: V202608 已发布
  - 步骤: `demand.build_gross(version_no=V202608)`
  - 期望: 抛 `FdeError` 含『已发布』或『不可合成』

- **TC-ERR-14 未合成即发布拦截**
  - 前置: 新建草稿版本 V202609（无 demand 行）
  - 步骤: `demand.publish(version_no=V202609)`
  - 期望: 抛 `FdeError` 含『未合成』

- **TC-ERR-15 草稿版本运算净需求拦截**
  - 前置: V202609 草稿
  - 步骤: `demand.calc_net(version_no=V202609)`
  - 期望: 抛 `FdeError` 含『未发布』或『草稿』

- **TC-ERR-16 已冻结版本重复运算净需求拦截**
  - 前置: V202608 已冻结
  - 步骤: `demand.calc_net(version_no=V202608)`
  - 期望: 抛 `FdeError` 含『冻结』或『不可重复』

### 3.5 需求池状态机分支

- **TC-ERR-17 非待下达状态下达拦截**
  - 前置: 取已下达补库单 RP001
  - 步骤: `demand_pool.release(replenish_no=RP001)`
  - 期望: 抛 `FdeError` 含『状态』（不可下达）

- **TC-ERR-18 非生产中状态入库回传拦截**
  - 前置: 取已下达补库单 RP002
  - 步骤: `demand_pool.on_inbound(replenish_no=RP002)`
  - 期望: 抛 `FdeError` 含『状态』（不可入库回传）

- **TC-ERR-19 已完成状态取消拦截**
  - 前置: RP001 已完成
  - 步骤: `demand_pool.cancel(replenish_no=RP001)`
  - 期望: 抛 `FdeError` 含『状态』或『取消』（终态不可取消）

### 3.6 三类补库生成（需求池 create 服务分支）

- **TC-ERR-20 缺货补库（产能紧张档）**
  - 步骤: `demand_pool.create(material_no=M1, replenish_type=缺货补库, replenish_qty=100, required_inbound=2026-08-20, stock_on_hand=0, min_level_a=0, safety_level_c=0, batch_level_b=0, capacity_tight=True)`
  - 期望: 返回 `replenish_no`、`status=待下达`、`replenish_type=缺货补库`

- **TC-ERR-21 最低库存补库（产能富余档）**
  - 步骤: `demand_pool.create(material_no=M1, replenish_type=最低库存补库, replenish_qty=200, required_inbound=2026-08-21, capacity_tight=False)`
  - 期望: 返回 `replenish_no`、`status=待下达`

- **TC-ERR-22 安全库存补库**
  - 步骤: `demand_pool.create(material_no=M1, replenish_type=安全库存补库, replenish_qty=300, required_inbound=2026-08-22)`
  - 期望: 返回 `replenish_no`、`status=待下达`

### 3.7 替换件合并 / 断点追溯（零件级处理验证）

- **TC-ERR-23 替换关系建立与合并**
  - 步骤:
    1. `md_part_replace.create(old_material_no=M3, new_material_no=M4, ecn_no=ECN-001)` → 返回 `rel_no`
    2. 新版本 V202609 下对 M3 填预测并 build_gross
  - 期望: 替换关系生效；`md_part_replace.list(status=生效)` 含 M3→M4；build_gross 将 M3 毛需求合并到 M4
  - 调用链验证: build_gross → `md_part_replace.list`

- **TC-ERR-24 断点追溯原物料链**
  - 步骤:
    1. `md_breakpoint.create(customer_no=C001, old_material_no=M3, new_material_no=M4, switch_time=2026-09-01, ecn_no=ECN-002)` → 返回 `bp_id`
    2. `md_breakpoint.trace(new_material_no=M4)`
  - 期望: `trace` 返回序列含 `M3`（沿断点追溯原物料链）
  - 调用链验证: trace（被 sales_forecast.calc_baseline 调用）

### 3.8 策略拟合状态机分支

- **TC-ERR-25 非待复核状态复核通过拦截**
  - 前置: FIT-202608 × M1 已生效
  - 步骤: `strategy_fitting.approve(fit_version=FIT-202608, material_no=M1)`
  - 期望: 抛 `FdeError` 含『待复核』或『状态』

- **TC-ERR-26 非待复核状态否决拦截**
  - 前置: 已生效记录
  - 步骤: `strategy_fitting.reject(fit_version=FIT-202608, material_no=M1)`
  - 期望: 抛 `FdeError` 含『待复核』或『状态』

- **TC-ERR-27 非已生效状态回滚拦截**
  - 前置: 待复核记录（M2）
  - 步骤: `strategy_fitting.rollback(fit_version=FIT-202608, material_no=M2)`
  - 期望: 抛 `FdeError` 含『生效』或『状态』

### 3.9 状态机正向转换与批量服务补充

- **TC-ERR-28 项目阶段正向推进（进行中→SOP）**
  - 步骤: `md_project.update(project_no=PRJ-1, stage=SOP)`
  - 期望: 返回 `stage=SOP`（单向流转正向推进）

- **TC-ERR-29 替换关系停用（生效→失效）**
  - 前置: §TC-ERR-23 建立的 M3→M4 替换关系
  - 步骤: `md_part_replace.disable(rel_no=REL1)`
  - 期望: `md_part_replace.get(rel_no=REL1)` 返回 `status=失效`

- **TC-ERR-30 待下达补库单取消（正向）**
  - 前置: §TC-ERR-22 生成的安全补库单（待下达）
  - 步骤: `demand_pool.cancel(replenish_no=RP003)`
  - 期望: `demand_pool.get(replenish_no=RP003)` 返回 `status=已取消`

- **TC-ERR-31 拟合否决（待复核→已否决）**
  - 前置: 对 M2 发起 `strategy_fitting.run(material_no=M2, fit_version=FIT-202608)` 得待复核记录
  - 步骤: `strategy_fitting.reject(fit_version=FIT-202608, material_no=M2)`
  - 期望: `strategy_fitting.get(fit_version=FIT-202608, material_no=M2)` 返回 `status=已否决`；`md_material.get(material_no=M2)` 未被回填

- **TC-ERR-32 拟合回滚（已生效→回滚上一版）**
  - 前置: M1 已有已生效记录（§TC-MC-18）且存在上一版参数
  - 步骤: `strategy_fitting.rollback(fit_version=FIT-202608, material_no=M1)`
  - 期望: 返回回滚结果，本记录 `status=已否决`，`md_material` 回填上一版参数

- **TC-ERR-33 事件调整（adjust_event）**
  - 前置: V202609 草稿，M2 已填预测
  - 步骤: `sales_forecast.adjust_event(version_no=V202609, material_no=M2, customer_no=C002, rolling_month=N+1, event_analysis=促销, event_adj=100)`
  - 期望: `sales_forecast.get(version_no=V202609, material_no=M2, customer_no=C002, rolling_month=N+1)` 返回 `event_adj=100`、`base_event_qty=100`

- **TC-ERR-34 库存策略批量计算（calc_batch）**
  - 步骤: `inventory_strategy.calc_batch(version_no=V202609)`
  - 期望: 返回批量计算结果（逐物料，单个失败不中断其余）

- **TC-ERR-35 推移表批量刷新（refresh_batch）**
  - 步骤: `inventory_projection.refresh_batch(biz_date=2026-08-15)`
  - 期望: 返回整批刷新结果

- **TC-ERR-36 拟合批量（run_batch）**
  - 步骤: `strategy_fitting.run_batch(fit_version=FIT-202608)`
  - 期望: 返回批量拟合结果（逐物料落待复核记录）

- **TC-ERR-37 月度版本取活跃版本（get_active）**
  - 步骤: `md_monthly_version.get_active()`
  - 期望: 返回当前活跃版本（`lock_status` ≠ 冻结）

- **TC-ERR-38 物料批量导入（import_batch upsert）**
  - 步骤: `md_material.import_batch(rows=[{material_no:M5, material_name:物料C, base_method:移动平均, base_params:{"window":6}}, {material_no:M1, material_name:物料A更新}])`
  - 期望: 返回 `created=1`、`updated=1`（M5 新增、M1 更新）

### 3.10 出库计划分支（outbound_plan，用新物料 M6 / 新客户 C003，不污染既有断言）

- **TC-OP-01 前置建数**：`md_customer.create(C003)` + `md_material.create(M6)`。
- **TC-OP-02 新建（未来日期→待出库）**：`outbound_plan.create(customer_no=C003, material_no=M6, qty=300, out_date=2026-09-20)` → `status=待出库`，`plan_no` 以 OB 开头。
- **TC-OP-03 新建（过去日期→直接已关闭）**：`out_date=2026-08-10` → `status=已关闭`。
- **TC-OP-04 校验分支**：非法物料/客户/数量≤0/非法日期均报对应 FdeError（主数据引用铁律 + 数量日期校验）。
- **TC-OP-05 列表过滤**：按 `material_no` / `status` AND 过滤，total 与状态分布一致。
- **TC-OP-06 延期恢复**：已关闭计划 `update(out_date=未来)` → 恢复 `待出库`。
- **TC-OP-07 回填实际出库单号**：`update(actual_out_no=…)` 后状态保持 `待出库`。
- **TC-OP-08 删除约束**：已关闭删除报「不可删除」；延期恢复 `待出库` 后可删。
- **TC-OP-09 close_expired 幂等**：过期单已在读取时结算，重复调用 `closed=0`。
- **TC-OP-10 推移表消费**：`inventory_projection.refresh(M6)` 后，各 `out_date` 当日 `outbound_qty` = 待出库计划数量合计（含延期恢复单），已关闭不计入。

---

## §4 待确认

- **Q1 基线/水位依赖历史数据（stub 空）**：`_load_sales_history` 当前 stub 返回空，导致 `base_qty=None`、库存水位 A/C/B=0、`decide` 走「基线未计算 → 取客户调整后值」分支。真实历史数据接入后：基线非 0、水位非 0、`decide` 的「MAPE 阈值 / 偏离率双源对比」分支（MAPE 小/大 × 偏离小/大）方可触达，届时需补用例覆盖该分支。
- **Q2 推移表击穿触发补库**：stub 下 `opening_stock=0`、出库为 0，推移表 `balance` 可能不击穿水位线，`scan_alert` 触发 `demand_pool.create` 的调用链在 §TC-MC-13 以「可查到补库单」为弱断言；三类补库的确定性覆盖由 §TC-ERR-20~22 直接调用 `demand_pool.create` 承担。
- **Q3 替换件合并/断点处理的完整链路**：§TC-ERR-23/24 验证了替换关系与断点追溯的建数+追溯；合并后的毛需求归属（M3 合并到 M4）依赖真实历史使预测非 0，当前以「build_gross 调用 md_part_replace.list」为调用链断言。
- **Q4 主计划 plan_version 递增**：§TC-MC-11 首次导入 plan_version=1；再次导入同物料 plan_version 应 +1（增量覆盖见第⑤步执行时补充）。

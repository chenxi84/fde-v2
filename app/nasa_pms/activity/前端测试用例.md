# 前端测试用例：进度活动台账（`activity`）

> 产出技能：第⑧步 前端测试用例（规格 `design-plus/前端测试.md`）
> 来源：`activity/前端详设.md`（A-1..A-7）与 `activity/应用详设.md`（BR-01..08 / 状态机）**同源**
> 执行：第⑨步 `python app/nasa_pms/tests/verify_view_nasa_pms_activity.py`（32 项，全绿）

---

## §0 自足造数（前置，走 REST 不经 UI）

| 造什么 | 结果 |
|---|---|
| WBS | `900000`（**父，故意留着子元素**）· `900000.01` / `900000.02`（两个叶子） |
| 一条已批准的变更 | `CR-001`（配置项 → 影响分析 → 提交审批 → 批准） |
| 一条单链网络 | `ACT-001` 起始里程碑（`phase=start`）→ `ACT-002` 5 天 → `ACT-003` 3 天 → `ACT-004` 完成里程碑 |
| 一条开口端 | `ACT-005`（无前置、无后续 —— 供体检断言） |

## §1 渲染与入口

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-ROUTE-01` | 打开 `#/activity` | 标题「进度活动台账」；台账 **5 行** |
| `VT-ROUTE-02` | 表头 | 含 活动编号 / 名称 / 类型 / 挂靠元素 / 工期 / 前置 / 状态 / 完成 / 基线 / 责任方 |
| `VT-ROUTE-03` | 入口 | 「新建活动」「新建里程碑」「网络体检」「排程视图」**都存在可见** |
| `VT-ROUTE-04` | 首行内容 | `ACT-004`：里程碑 / 计划 / **未基线** |

## §2 网络体检

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-CHECK-11` | 点「网络体检」 | 面板出现；问题合计 = **2**（ACT-005 缺前置 + 缺后续） |
| `VT-CHECK-12` | 开口端明细 | 点名 `ACT-005` |

## §3 排程视图（派生）

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-SCH-21` | 切换视图 | 排程表出现；派生日期形如 `YYYY-MM-DD` |
| `VT-SCH-22` | 正推 | **链内**日期递增（`ACT-001 ≤ ACT-002 ≤ ACT-003 ≤ ACT-004`）；⚠ 不判"全表递增"——开口端那条本来就更早 |
| `VT-SCH-23` | 浮时与关键路径 | 单链四条浮时全 0 且标「关键路径」；**开口端那条不在关键路径上** |
| `VT-SCH-24` | 标注 | 含「派生排程」字样（日期由工期与逻辑链正推） |

## §4 新建活动与里程碑

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-FORM-31` | 不选挂靠元素 | 被拒（toast 含「挂靠」），行数不变 |
| `VT-FORM-32` | **BR-05** 挂**父**元素 | 被拒（toast 含「最底层元素」） |
| `VT-FORM-33` | 正常新建 | 成功，台账 6 行且出现 `ACT-006` |
| `VT-FORM-34` | **BR-04** 里程碑带工期 | 被拒（toast 含「必须为 0」），行数不变 |
| `VT-FORM-35` | **BR-06** 名称重复 | 被拒（toast 含「同名活动」） |

## §5 连前置

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-LINK-41` | 给 `ACT-005` 连 `ACT-001` | 成功，前置列出现 `ACT-001` |
| `VT-LINK-42` | **BR-02** 冗余链接 | 给 `ACT-003` 连 `ACT-001`（A→B→C 时 A→C）→ 被拒（toast 含「冗余链接」） |

## §6 基线 / 修订 / 实绩

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-ACT-51` | 基线 | 有回执（toast 含「已基线」+ 日期区间）；基线列出现 `→` |
| `VT-ACT-52` | 锁定 | 该行「基线」隐藏、「发起修订」出现 |
| `VT-ACT-53` | 详情里的编辑 | 已基线活动**看不到**「编辑」；提示指向「发起修订」 |
| `VT-ACT-54` | 变更号候选 | 只列**已批准**的（`CR-001`） |
| `VT-ACT-55` | 发起修订 | 变更号列出现 `CR-001` |
| `VT-ACT-56` | 回填实绩 | 100% + 实际完成 → 状态「已完成」 |
| `VT-ACT-57` | **BR-08** 不动基线 | 回填前后**基线列一字未变** |

## §7 硬件指标

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-HW-90` | 全程 | **0 console error / 0 pageerror / 0 HTTP≥400**（豁免清单逐条受检） |

## §8 甘特视图（2026-09-26 增补）

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-GANTT-71` | 点「甘特视图」 | 甘特区可见；行数 = 台账行数 |
| `VT-GANTT-72` | 条形 | 每条活动都有条形且宽度 > 0 |
| `VT-GANTT-73` | 关键路径 | `data-crit="1"` 的条数在 0 与总数之间（部分关键） |
| `VT-GANTT-74` | 切回台账 | 视图切换正常 |

## §9 四种关系与日历（2026-09-26 增补）

| 编号 | 用例 | 预期 |
|---|---|---|
| `VT-REL-81` | 连前置的关系类型 | **四个**静态选项（FS/SS/FF/SF） |
| `VT-REL-82` | **§5.5.8.2** 非 FS 缺理由 | 选 SS 不填理由 → 前端拦下（toast 含「必须写明理由」） |
| `VT-REL-83` | SS + 滞后 + 理由 | 有回执；台账**前置列显示** `ACT-006·SS+2`（关系与滞后可见） |
| `VT-CAL-91` | 日历候选 | `#act-cal-options` 非空，输入框已选默认日历 |
| `VT-CAL-92` | 排程标注 | `[data-role="sch-cal"]` 含「口径」与 `CAL-` |

> ⚠ `VT-REL-82/83` 的候选必须挑**独立分支**的活动（如 `ACT-006`）：挑同链上已间接前置的活动会被判
> **冗余链接**（BR-02）—— 那是闸门正确，不是缺陷。本组的 e2e 就在这里先撞了一次。

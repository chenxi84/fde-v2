# 前端详设：工作分解结构台账（`wbs`）

> 产出技能：第⑥步 前端设计（规格 `design-plus/前端设计.md`，规范正本 `VIEW_CONVENTION.md`）
> 输入：`wbs/应用详设.md`（状态机 / 字典字段 / BR-01..07）+ 材料 `brd/WBS-第*.md`
> 落点：`app/nasa_pms/wbs/view.js` + `view.html`（PAGE_META.key = 目录名 = `wbs`）

---

## 1. 页面定位

| 项 | 内容 |
|---|---|
| key / name | `wbs` / **WBS** |
| `order` | `720`（本组 12 个应用里最后一位 —— 与 `architecture.md` ① 的聚合根序号同序） |
| 一句话 | 一棵**编号自洽的元素树** + 每个元素的字典条目；基线之后改动走变更 |
| 数据来源类型 | **独立创建**（顶层元素自带 6 位编号；子元素由系统按规则分配子号） |

## 2. 页面结构

```
┌ 工具条：状态筛 · 类型筛 · 关键词 · [树视图/列表视图] · [新建顶层元素] ────┐
├ 列表视图：15 列（编号 / 名称 / 类型 / 层级 / 父元素 / 状态 / 版次 / 责任方 /
│            范围定义出处 / 关联需求 / 内容描述 / 规范号 / 预算与报告号 / 修订授权 / 操作）
├ 树视图：`[data-role="tree"]` 缩进索引（按 depth × 26px 缩进 —— 材料 §3.4.4 图 3-12）
└ 四个模态：详情/编辑 · 新建（顶层或子元素）· 发起变更 · 关闭
```

## 3. 交互规则（逐条对应后端约束）

| # | 规则 | 依据 |
|---|---|---|
| W-1 | **子号不给填**：`add_child` 由系统分配（`父号.01/.02…`）；顶层才给编号输入框 | BR-01（编号规则机械可判，不该交给人） |
| W-2 | 「纳入基线」按钮**在前端预禁用**（无范围出处时）—— 前端只做提示，**判据在后端**（后端 BR-03 另有硬闸） | BR-03；本组惯例：前端镜像 ≠ 判据 |
| W-3 | 「发起变更」的变更号候选**只列已批准**（`change_request.list(status=approved)`）；值仍是文本，后端 `change` 会再校验一次 | BR-05（§3.3.6） |
| W-4 | 已基线元素**不给「编辑」入口**（给的是「发起变更」）—— 否则点进去字段全灰，是死胡同 | BR-05；同组 `decision` 页对 frozen 状态同款处理 |
| W-5 | 已关闭是终态：按钮只剩「详情」 | BR-06 |
| W-6 | 动作按钮用 `x-show` 控制（元素仍在 DOM），**e2e 断言存在性必须按可见性判** | pitfalls：`x-show` 不等于不存在 |
| W-7 | 详情模态里的字段用 `data-role` 钩住 | 整卡 `inner_text` 会被 `select` 的全部 `option` 文本污染（本组踩过） |

## 4. 字段展示（业务名取自术语表）

| 列 / 字段 | 业务名 | 备注 |
|---|---|---|
| `wbs_no` | 元素编号 | 等宽字体；**不可变**，界面上没有输入框（BR-07） |
| `title` | 元素名称 | 提示「必须按产品分解」 |
| `level` / `parent_no` | 层级 / 父元素 | 顶层显示「（顶层）」 |
| `kind` | 类型 | 产品 / 使能性工作 / 控制账户 / 工作包 / 规划包 |
| `scope_ref` | 范围定义出处 | 没有它不能基线（BR-03） |
| `req_nos` | 关联需求 | 交叉引用矩阵的 X（§3.3.3），覆盖对账按它求差 |
| `charge_code` | 预算与报告号 | §3.4.4h |
| `rev_no` / `rev_authorization` | 版次 / 修订授权 | 落实变更时 +1 并记变更号 |
| `status` | 状态 | 草稿 / 已基线 / 变更中 / 已关闭（语义色） |

## 5. 权限

平台注入 `ctx`，应用不实现鉴权；页面授权走「页→服务」隐式放行（`X-Fde-Page`）。本页挂在
`nasa_planner` 角色名下（见 `_roles.py`）。

## 6. 依赖的主数据 / 跨应用

| 依赖 | 用途 | 在哪一层 | 失败时 |
|---|---|---|---|
| `stakeholder.list` | 「责任方」候选（**值仍是文本姓名**） | **视图层** `svc(...)` | 静默兜底为空列表（不喷 toast） |
| `change_request.list(status=approved)` | 「发起变更」的变更号候选 | **视图层** | 同上 |

> ⚠ 这两条是**视图层**依赖（只影响"怎么填"），后端另做校验的是 `wbs → change_request.get`
> 与 `wbs → requirement.list`（见 `architecture.md` 的跨应用调用方向表，由 `verify_app_edges.py` 对账）。

## 7. 已知限制

- **不画图形化的元素树**（材料 §3.4.3 的 tree diagram）：本页给的是**缩进索引**（§3.4.4 的 WBS Index），
  图形化树留白 —— 意义有限而维护成本高
- **不做拖拽调整层级**：改层级 = 改编号 = 换一个元素（BR-07），没有"拖一下"这种入口
- **覆盖对账不在本页展示**：`coverage` 是服务级能力，页面留白（避免与看板的读数重复）

## 覆盖度

| 详设条目 | 前端落点 |
|---|---|
| 状态机四态 | 徽标 + 动作按钮显隐（W-2/W-4/W-5） |
| BR-01 / BR-07 | 子号自动分配、编号无输入框（W-1） |
| BR-03 | 「纳入基线」预禁用（W-2） |
| BR-05 | 变更号候选只列已批准 + 已基线无「编辑」（W-3/W-4） |
| BR-06 | 草稿/变更中无「关闭」、终态只剩详情（W-5） |
| 字典字段（§3.4.4 a–h） | 表格 15 列 + 详情模态全字段 |

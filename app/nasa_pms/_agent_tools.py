"""nasa_pms 组级 Agent 工具声明（可选的第二份声明；格式见 `design-plus/智能体角色声明.md` §七）。

与 `_roles.py` 同目录、同约定：平台扫描装配，单份出错**仅跳过该组**。
**只影响 Agent 的工具表，不影响 `self.fde.call`** —— 应用之间该怎么调还怎么调。
"""

# ① 哪些服务**不给 Agent 调**（黑名单）。
#
# 判据（规格 §7.1）：只摘**明确特征**的三类 —— 内部回填 / 外部回执(同步入口) / 被 batch 版取代的单行版。
# ⚠ **不确定的宁可不摘**：工具面膨胀是概率问题，功能缺失是确定问题。
HIDDEN_FROM_AGENT = {
    # 外部同步入口：相关方主数据**来自外部**（机构与人员系统），本系统"只读引用、不维护源数据"
    # （聚合根卡 11 + `stakeholder` 应用详设 §2）。让 Agent 能 upsert 等于让它可以**凭空发明相关方**,
    # 与主数据的来源口径冲突；要同步请走外部系统/集成接口。
    "nasa_pms/stakeholder": ["upsert"],
}

# ② 组描述提示：**什么时候该用这个应用、以及别跟谁搞混**。
#
# 为什么需要（规格 §7.2）：组描述默认取前端 `PAGE_META` 的中文名 + crumb，而本组 11 个应用的
# crumb 高度同构（都是「xx 台账 · 状态A · 状态B」这种），工具面收敛后实测**最大的新增失败模式
# 就是"开错组"**。每条写成「用途一句话 + 查 X 用它、查 Y 用『另一个』」。
GROUP_HINTS = {
    "nasa_pms/activity":
        "**IMS 里的活动与里程碑**（工期 + 逻辑链；日期是**派生**的，不是填的）。"
        "挂靠必须落到 WBS 的**叶子**元素上；连前置用**四种关系**（FS/SS/FF/SF）+ 滞后，"
        "非 FS 要写理由，且禁冗余、禁成环；"
        "工期口径与假日表在**日历**上（工作日 / 日历天两套口径，材料要求全表一个口径）；"
        "基线后改工期要走变更流程，但**回填实绩不受影响**（它不动基线）。"
        "想知道「哪条在关键路径上、什么时候完」用 `activity.schedule_view` / `critical_path`。",
    "nasa_pms/wbs":
        "**项目工作按产品分解的那棵树**（元素编号是点分十进制层级码，最多 7 层）。"
        "基线之后改动要走变更流程（先 change 再 baseline 落实）；"
        "查「这项工作覆盖了哪些需求、还有哪些没覆盖」用 `wbs.coverage`；"
        "**不要手写子编号** —— 用 `add_child`，子号由系统按规则分配。",
    "nasa_pms/requirement":
        "**要满足什么**（技术要求、派生链、基线）。查「怎么验、验完没有」用『验证项』；"
        "查「谁来承担」用『利益相关者』（责任人字段是文本，主数据在那里）。",
    "nasa_pms/verification":
        "**验证矩阵的一行** = 需求 × 验证方法 × 阶段 × 结果。规划/执行验证用它；"
        "**需求本身**（可验证陈述与基线）在『需求』。判定为「不通过」**仍然算已覆盖** —— 覆盖读数回答"
        "「有没有验过」，不回答「好不好」。",
    "nasa_pms/risk":
        "**不确定事件**：等级由 可能性 × 后果 推导（不由人填）、缓解措施、关闭条件。"
        "查「指标实测值超没超阈值」用『技术度量』，不是这里。",
    "nasa_pms/technical_measure":
        "**量化指标**（TPM/MOP/KPP）的目标值 / 阈值 / 实测值序列；超阈值**同事务**开告警。"
        "查「不确定事件与缓解」用『风险』；查「选哪个方案」用『决策』（决策的 `measure_no` 指向这里的度量）。",
    "nasa_pms/decision":
        "**一次技术决策**：准则清单 + 备选方案 + 打分 + 结论（选非最高分必须给依据）。"
        "打分用的**度量**在『技术度量』、依据的**评审**在『评审』。",
    "nasa_pms/change_request":
        "**对配置项/基线的变更申请**：提交 → 影响分析 → 审批 → 实施。"
        "「配置项当前是什么版本、属于哪条基线」在『配置项』；改配置项内容前**先要有批准的变更请求**。",
    "nasa_pms/configuration_item":
        "**受控的工作产品**（软件/文档/模型/数据/硬件）：受控 → 纳入基线 → 发版 → 版本递增 → 归档。"
        "「为什么要改」在『变更请求』；版本号只能递增、已发布版本不可改写。",
    "nasa_pms/interface":
        "**两个系统之间的接口约定**（ICD/IRD/IDD/ICP）：定义中 → 已发布 → 冻结 → 版本变更。"
        "接口可挂配置项（`ci_no`）；**接口冻结**与配置项发版是两件事。",
    "nasa_pms/tech_plan":
        "**计划类文档**（SEMP / 验证计划 / 集成计划 / 技术评估 / HSI / 风险 / 评审 / CM）：草稿 → 审批中 → "
        "已批准 → 修订（版本台账）。**只有已批准的计划才能修订**；查「评审开了没」用『评审』。",
    "nasa_pms/review":
        "**一次技术评审**（SRR/PDR/CDR/TRR…）：评审项清单 + 结论 + 行动项跟踪。"
        "结论后内容**冻结**、已关闭是**硬终态**；评审依据的**计划**在『技术计划』。",
    "nasa_pms/stakeholder":
        "**主数据**：相关方（用户方/承包商/内部机构）及其**期望**（need/goal/objective/MOE/constraint）。"
        "来源是外部同步 ⇒ **本系统只读引用**（`upsert` 已对 Agent 摘除）。查「要求什么」用『需求』。",
}

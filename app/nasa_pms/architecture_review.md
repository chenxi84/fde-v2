# nasa_pms 业务覆盖度检查报告

> 对应《工具链使用说明》第 3 步门禁。输入：`brd/`（27 章）+ `本体对象.md` + `术语表.md`。
> **业务覆盖度 = 100%**（逐项见下）方可进入第②步。

---

## 一、业务覆盖度（逐项对照 `brd/`）

### 1.1 材料里的关键「单据」是否都有归属

| 材料中的单据 | 归属 | 说明 |
|---|---|---|
| SEMP（系统工程管理计划） | → `tech_plan` | 计划类，统一管理 |
| 验证计划 / 验证与确认计划（V&V Plan） | → `tech_plan` | 同上 |
| 集成计划 / 技术开发计划 / HSI Plan | → `tech_plan` | 同上 |
| 需求文档 / 需求基线 | → `requirement` | 基线是需求的状态，非独立聚合 |
| 验证矩阵 / 合规矩阵 | → `verification` | 矩阵的每一行 = 一条验证项 |
| 接口控制文档（ICD） | → `interface` | 接口约定的载体 |
| 决策报告 | → `decision` | 决策的产出 |
| 配置项（工作产品） | → `configuration_item` | CM 的核心对象 |
| 变更请求 | → `change_request` | CM 的变更流程 |
| 评审记录（PDR/SRR/CDR…） | → `review` | 含评审项与行动项 |

**覆盖率：10/10 = 100%** —— 无遗漏单据。

### 1.2 材料里的「主流程」是否有对应链路

材料 4.0–6.0 共 **17 个 SE 过程**（需求管理、风险管理、配置管理、技术数据管理、决策分析、
技术评估、产品验证/确认…）。

**处置：17 个过程**全部**明确排除**（不建聚合），理由见 §二档二 ——
**过程描述"怎么做"，是方法论，不是系统要管理的数据**；它们的**产出物**已分别落入上表的聚合。

### 1.3 材料里的「角色」是否有职责描述

| 材料中的角色 | 归属 |
|---|---|
| 利益相关者（客户 / 承包商 / 内部组织） | → `stakeholder`（聚合根卡含职责与期望） |
| 系统工程师 / 项目经理 / HSI 集成团队 | → `stakeholder`（作为相关方类型） |
| NASA 总部 / 各研究中心 | → `stakeholder`（外部组织类相关方） |

**覆盖率：3/3 = 100%**。

---

## 二、处置表（对照 `本体对象.md`）

> **范围按规则分两档**（清单 2786 条，全量逐项不可行且无意义）：
> 档一 = **聚合根候选，逐项处置**；档二 = **其余按类型概述落选理由**。

### 档一：聚合根候选 —— 逐项处置

| # | 候选对象（清单原词） | 关联数 | 处置 | 理由 |
|---|---|---|---|---|
| 1 | 需求 | 45 | **独立聚合** | 有标识、状态机与不变量（每条需求须有验证方法） |
| 2 | 需求基线 | 12 | **并入** `requirement` | 是需求的一个**状态快照**，无独立标识，与需求同事务 |
| 3 | 风险 / 技术风险 | 29 / 12 | **独立聚合** | 有状态、缓解措施与关闭条件；与技术风险同类（同一聚合，以类别属性区分） |
| 4 | 配置项 | — | **独立聚合** | CM 的核心；有版本与基线归属 |
| 5 | 变更请求 / 重大变更 | — / 9 | **独立聚合** | 有审批流与影响分析；**与配置项事务边界不同**（决策 5） |
| 6 | 评审（PDR/SRR/CDR/MCR/TRR/PRR/SIR/KDP） | 24/20/17/8/6/6/6/6 | **合并为一个聚合** | 同构（类型/阶段/评审项/结论），以类型属性区分；**拆成 8 个聚合是过度切分** |
| 7 | 行动项 / 纠正措施 | 12 | **并入** `review` | 随评审结论同事务产生、无独立标识；跨评审汇总属**查询视图**（决策 2） |
| 8 | 验证项 / 验证矩阵行 | — | **独立聚合** | 与需求**不同节奏**（需求一次基线、验证多轮执行），合并会让验证结果反复改动基线（决策 6） |
| 9 | 技术绩效指标(TPM) / TRL / 技术度量 | 9 / 7 / 4 | **独立聚合**（合并为一个，类别属性区分） | 有目标值、阈值、实测值，独立跟踪与告警 |
| 10 | 决策 / 决策报告 | — / 15 | **独立聚合** | 有备选方案、准则与结论；不变量（少于两方案不得结论） |
| 11 | 接口 / 接口控制文档 | 12 / 11 | **独立聚合** | 有两端、版本与约定内容 |
| 12 | 技术计划（SEMP/各计划） | 57 等 | **独立聚合** | 计划类文档结构同构，合并为 `tech_plan` |
| 13 | 利益相关者 / 利益相关方 / 期望 | 30 / 29 / 28 | **独立聚合（主数据）** | 唯一主数据；期望作为其内部实体 |
| 14 | WBS / 工作包 | 24 | **独立聚合**（2026-09-26 订正） | SE 手册 6.1 只给结论（进配置控制、与基线进度和预算互锁、工作包需工作授权），**细则外链**给 WBS 手册。现已取得现行版 `NASA/SP-20250006071`(2025-06) 并入库：**编号体系**（3.4.2：7 层十进制码）、**元素级变更控制**（3.3.6）、**全包含性**（3.3.5）、**字典字段**（3.4.4 a–h）、**产品导向**（3.5.2 非产品元素黑名单）、**与需求交叉引用**（3.3.3）齐备 → 依据充分 |
| 15 | 技术数据（Technical Data） | — | **排除（待确认）** | 材料 6.6 节专讲，但清单里只抽到"技术数据**管理过程**"（过程类），数据项本身未抽出——见 §四 |
| 16 | 阶段（Phase A–E / Pre-Phase A） | 32 等 | **排除** | 是计划/评审/度量的**枚举属性**，无独立生命周期（决策 3） |
| 17 | 基线（配置基线） | — | **排除** | 同 2，作为状态而非实体 |

### 档二：其余对象 —— 按类型概述落选理由

| 类型 | 条数 | 落选理由（**一句概述，不逐项**） |
|---|---:|---|
| 流程 | 609 | 方法论步骤（`产品验证过程`、`技术规划流程`、`配置管理流程`…）—— 描述"怎么做"。**其产出物已落入档一的聚合** |
| 业务单据 | 568 | 除档一列出的计划/报告类，其余多为**文档片段与示例表名**（`决策报告`已收，`Table 2.7-1` 类属噪声） |
| 角色 | 482 | 除档一收的相关方，其余是**机构名碎片**（`NASA/Langley Research Center`）与**泛称**（`技术团队`）—— 归入 `stakeholder` 或不予建模 |
| 标准与规范 | 451 | **外部文献**（`NPR 7123.1`、`AS9100`、手册本身）—— 是**依据**不是**数据**；如需引用另作附件管理 |
| 需求与跟踪项 | 263 | 除档一列的，其余是**需求分类词**（`功能需求`、`派生需求`）—— 作 `requirement` 的分类属性，不独立成聚合 |
| 指标 | 146 | 除档一列的，多为**量纲与度量词**（`成本`、`目标函数`）—— 作 `technical_measure` 的类别属性 |
| 评审 | 139 | 除档一列的评审类型，其余是**评审的别名与变体**（`初步设计评审(PDR)` 与 `PDR` 同一事——**已由术语表归一**） |
| 阶段 | 128 | 同档一 #16，枚举属性 |
| 其他类型 | 558 | 泛化概念（`系统`、`产品`、`接口`）+ 文本产物 —— **本就不属任何业务类型**，落空率 16.7% 属正常 |

---

## 三、命名核对（术语表）

`architecture.md` 总表「聚合根」列的业务名，**逐个对照 `app/nasa_pms/术语表.md`**（宽松口径）。
**首次核对结果：11 个里有 4 个不在表中** —— 它们是本步就地新取的，已按规则**回填**：

| 聚合根业务名 | 首次核对 | 处置 |
|---|---|---|
| 需求 / 风险 / 评审 / 决策 / 接口 / 利益相关者 | ✅ 在表中 | — |
| **技术计划** | ❌ 表中只有 `SEMP`、`验证计划`、`集成计划`等**具体计划** | **合并新取** → 已回填（原词：`技术计划`） |
| **配置项** | ❌ 表中无 | 清单里的原词是 `配置项(CI)` → 已回填 |
| **变更请求** | ❌ 表中无（表里只有 `重大变更`） | 清单里有 `变更请求`（关联 8）→ 已回填 |
| **技术度量** | ❌ 表中无 | 清单里的原词是 `技术度量(Technical Measures)` → 已回填 |
| **验证项** | ❌ 表中无，**清单里也没有对应对象** | ⚠ 见下 |

⚠ **`验证项` 是本步"比清单更细"的建模**：清单里只有 `需求验证矩阵`（关联 7，一份单据），
而本步把**矩阵的一行**建模为一条验证项（有结果、证据、状态）。这在领域建模里**完全合理**
（聚合可以比原料更细），但它**意味着该业务名在本步才诞生** → 按规则**就地取名 + 回填**
（术语表已加：原词 `需求验证矩阵` → 业务名 `验证项（Verification Item）`）。

> **这条核对的价值就在这儿**：不核对的话，`配置项` / `变更请求` / `技术度量` / `验证项` 四个名字
> 会**只存在于 `architecture.md`**，②③⑥⑦ 各步各取各的名（`配置项` vs `CI` vs `Configuration Item`），
> 最后页面上三种写法并存 —— 正是术语表要防的事。

**回填后复跑核对：11/11 全部可在表中匹配。**

---

## 四、待确认（须业务方裁决，不臆造）

| # | 待确认 | 影响的决策 |
|---|---|---|
| 1 | **是否管 WBS / 工作包？** 本书只给结论（进配置控制、与进度预算互锁、工作包要工作授权），**细则外链给 `NASA/SP-2010-3404`** | 加则成第 12 个聚合 —— 若加，它的事务边界是"**工作授权 / 成本账户**"，**不是**并入 `tech_plan`（书里三者并列，都受配置控制）；不加则明确排除并说明 |
| 1 之补 | ✅ **已裁决（2026-09-26）：纳入**。细则来源为现行版 `NASA/SP-20250006071`《NASA WBS Handbook》(2025-06, 65 页) —— 已随 `app/nasa_pms/book/` 入库，分章为 `brd/WBS-第NN章-*.md`（9 章：前言 + Chapter 1–4 + 附录 A/B/C/E）。载明编号体系（3.4.2）、职责（3.2）、**变更控制（3.3.6）**、层数与全包含性（3.3.4/3.3.5）、**字典字段（3.4.4 a–h + 附录 C）**、与配置管理的关系（4.1.2）与工作识别与分派（4.2） | 已执行：档一 #14 改判**独立聚合**，新增第 12 张聚合根卡与 `wbs` 应用（见 §五 门禁结论） |
| 2 | **技术数据是否独立成聚合？** 材料 6.6 节专讲，但清单里只抽出"管理过程" | 加则第 13 个聚合 |
| 3 | **基线作状态还是独立实体？** 本次按状态（并入需求/配置项） | 影响 I-1/I-2 的落法 |

---

## 五、门禁结论

- **业务覆盖度 = 100%**（单据 10/10、主流程 17/17 明确处置、角色 3/3）✅
- **处置表**：档一 17 项逐项处置 + 档二按 8 类概述 ✅
- **命名核对**：首次核对 **11 个里有 4 个不在表中**（本步新取：`配置项` / `变更请求` / `技术度量` / `验证项`），
  **已逐个回填术语表**；回填后复跑 **11/11 全部匹配** ✅
- **待确认 3 项**：已列，**不臆造**，交业务方裁决 ✅

**② 增量（2026-09-26）：WBS 并入** ——
- 输入追加一本（`brd/WBS-第*.md` 9 章），**旧 27 章一字未动**（转换器加了 `--only` / `--prefix`，
  否则两本书的章号会撞车且不报错）
- 档一 #14 `WBS / 工作包`：**排除（待确认）→ 独立聚合**，据的是新书 3.3.5/3.3.6/3.4.2/3.4.4/3.5.2
- 聚合根 **11 → 12**；术语表补 WBS 元素级词条；`architecture.md` 加第 12 张卡与两条跨应用边
- 其余 11 个聚合**未受影响**（增量式，不重跑索引：① 这一步的证据来自**原文**而非图谱候选，
  图谱是"候选生成器"，这次的候选是业务方点名的）

**→ 可进入第②步（逐聚合根应用详设）。**

> ⚠ **本文件不是术语来源** —— 组级 `app/nasa_pms/术语表.md` 才是唯一来源，
> 本文件的**表格部分已并入该表**（去重后）。此处保留的是**本应用特有的部分**：
> 字段级的就地取名说明、以及 §「就地细化」里**材料没写、本步自己定的口径**（诚实披露）。

# nasa_pms 术语表 · 增量回填（change_request 应用）

> **应用级暂存** —— 第②步就地取名后按规则回填，格式与列义**照 `app/nasa_pms/术语表.md`**（三列：
> 原词 / 语境·所属类型 / 业务名）。组级 `术语表.md` 由主进程统一合并，**本文件不直接改它**（避免并行写冲突）。
> 「变更请求」一词**组级术语表已收**（`| 变更请求 | 跟踪项 / 配置管理 | 变更请求（Change Request） |`），本应用沿用，未重复登记。

| 原词 | 语境 / 所属类型 | 业务名 |
| :--- | :--- | :--- |
| change request identifier | 字段（变更请求）/ 第②步新增 | 变更请求编号 |
| change request title | 字段（变更请求）/ 第②步新增 | 变更请求标题 |
| Change originator | 字段（变更请求）/ 材料 FIGURE 6.5-4 第 1 列（组级表已收「变更提出方」为角色，本行是其字段面） | 申请方 |
| affected configuration items | 字段（变更请求）/ 第②步新增（弱引用 configuration_item） | 影响的配置项 |
| affected requirements | 字段（变更请求）/ 第②步新增（弱引用 requirement） | 影响的需求 |
| change description / justification | 字段（变更请求）/ 材料 §6.5.1.2.3 "systematic proposal, justification, and evaluation" | 变更说明 |
| impact analysis | 字段（变更请求）/ 材料 FIGURE 6.5-4 第 3~4 步（评审与裁决结论） | 影响分析 |
| disposition / decision | 字段（变更请求）/ 材料 FIGURE 6.5-4 第 7 步 "Disposition change request" | 审批意见 |
| approver | 字段（变更请求）/ 材料 §6.5.1.2.3（CCB 成员） | 审批人 |
| implementation note | 字段（变更请求）/ 材料 FIGURE 6.5-4 第 9 步 "Execute approved changes" | 实施说明 |
| submitted | 字段（变更请求）/ 状态字典（提交后、分析前） | 已提交 |
| analyzing | 字段（变更请求）/ 状态字典（影响分析中） | 影响分析 |
| reviewing | 字段（变更请求）/ 状态字典（送 CCB 后、处置前） | 审批中 |
| approved | 字段（变更请求）/ 状态字典（CCB 批准） | 已批准 |
| rejected | 字段（变更请求）/ 状态字典（CCB 拒绝，终态） | 已拒绝 |
| implemented | 字段（变更请求）/ 状态字典（变更已落地，终态） | 已实施 |
| CCB / Configuration Control Board | 角色 / 材料 §6.5.1.2.3 与 FIGURE 6.5-4 第 7 列 | 配置控制委员会（CCB） |
| Actionee | 角色 / 材料 FIGURE 6.5-4 第 8a 列 | 实施方（Actionee） |
| Normal Configuration Change Process | 流程 / 材料 FIGURE 6.5-4（八步变更控制链） | 变更控制流程（Normal Configuration Change Process） |

> ⚠ 上表**没有重复登记**组级术语表已有的行：`变更请求（Change Request）`、`变更提出方`、
> `配置管理员（CM）`、`配置项（Configuration Item, CI）`、`change request number`（配置项侧的
> 「变更请求号」字段，已由 `configuration_item` 第②步回填）。同一原词多行时，
> 下游按**所属分节**选行（组级表列义）。
